---
phase: 01-baseline-verification-net
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - packages/apps/service-worker/e2e/fixtures.ts
  - packages/apps/service-worker/e2e/project-host.spec.ts
  - packages/apps/service-worker/playwright.config.ts
  - scripts/verify/e2e-prerequisite.js
  - packages/apps/editor/editor-artifact-plugin.test.ts
autonomous: true
requirements:
  - VERIFY-06
  - VERIFY-07
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No e2e test in the workspace decides at runtime to skip itself: the editor-dependent service-worker test requests a required fixture that fails with a named remediation message when the prerequisite is absent."
    - "The required `editorRelease` fixture resolves when a staged editor release is resolvable and throws only when it is not: it reads the service worker's `editor.status` value (the same value MainView's ProjectView reads before it renders `open-editor`) from the app root, where no project is needed."
    - "Running the service-worker e2e spec with the editor release unstaged fails with the message `Editor release is not staged` rather than reporting a skip, and the default staged run of the same spec passes."
    - "The conditional assertion block in the same spec is an explicit, annotated decision instead of a silent weakening."
    - "The editor artifact manifest's `runtimeProtocolVersion` is pinned to its own literal 3 by a test that drives the real plugin, and `RUNTIME_PROTOCOL_VERSION` stays pinned to 4 — neither value is changed."
  artifacts:
    - "packages/apps/service-worker/e2e/fixtures.ts"
    - "scripts/verify/e2e-prerequisite.js"
    - "extended coverage in packages/apps/editor/editor-artifact-plugin.test.ts"
  key_links:
    - "the fixture asserts the release is staged before the test body runs, at setup rather than per-test, by requesting the service worker's `editor.status` message from the app root — the exact value that gates `open-editor`, so the fixture succeeds iff the affordance can appear"
    - "the verifier asserts the staged polarity first (default configuration, no MOTA_WITH_EDITOR override) so the fixture is proven to resolve when the prerequisite is present, then the unstaged polarity (MOTA_WITH_EDITOR=0, whose `pnpm build` re-runs `vite build` and empties `dist`) so the failure is genuine rather than inherited from a staged server"
    - "the verifier forces a fresh preview server on both polarities (CI=1 disables reuse), so neither run can accidentally reuse a server started for the other"
    - "the manifest assertion drives editorArtifactPlugin closeBundle on a fabricated artifact, so it observes the written manifest rather than a source literal"
---

<objective>
Remove the last silent e2e skip in the workspace (D-16: an e2e that lacks its prerequisite must fail, not silently pass) and pin the manifest protocol constant that is currently asserted nowhere.

Purpose: GitHub counts a skipped required check as success, so `test.skip(!withEditor, ...)` in `packages/apps/service-worker/e2e/project-host.spec.ts` makes a whole class of editor-hosting behaviour invisible to the merge box (VERIFY-06). Separately, `editor-artifact-plugin.ts` emits `runtimeProtocolVersion: 3` while `src/runtime/protocol.ts` exports `RUNTIME_PROTOCOL_VERSION = 4`; the milestone's Out-of-Scope table forbids "fixing" that drift, so VERIFY-07 asks only for a generated assertion that records both values unchanged.
Output: a required Playwright fixture that resolves against a staged release and fails loudly without one, a deterministic two-polarity verifier, and the manifest-side half of the protocol assertion.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-RESEARCH.md
@.planning/phases/01-baseline-verification-net/01-PATTERNS.md
@.planning/phases/01-baseline-verification-net/01-VALIDATION.md
@packages/apps/service-worker/e2e/project-host.spec.ts
@packages/apps/service-worker/playwright.config.ts
@packages/apps/editor/editor-artifact-plugin.ts
@packages/apps/editor/src/runtime/protocol.test.ts
@packages/apps/editor/editor-artifact-plugin.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Replace the silent skip with a required fixture that fails loudly</name>
  <files>packages/apps/service-worker/e2e/fixtures.ts, packages/apps/service-worker/e2e/project-host.spec.ts, packages/apps/service-worker/playwright.config.ts</files>
  <read_first>
    - packages/apps/service-worker/e2e/project-host.spec.ts (the `withEditor` predicate, the conditional block at lines 55-59, the `test.skip` at line 102, and `registerOpfsProject` at lines 5-43 — the repo's existing raw service-worker `postMessage([id, type, payload])` pattern)
    - packages/apps/service-worker/src/view/MainView.tsx (ProjectView's `useQuery(["editor-host"], …GetEditorHostStatusMessage…)` at lines 246-248, and the `open-editor` render gate `editorStatus.data?.status === "ready"` at lines 408,421-432 — this is why `open-editor` is absent on the home route)
    - packages/apps/service-worker/src/view/routes.ts (`currentViewRoute`: only `service/:id/project/` is a project route; the home route is everything else)
    - packages/apps/service-worker/src/idl/index.ts (`GetEditorHostStatusMessage` = `new MessageType<void, EditorHostStatus>("editor.status")` and the `EditorHostStatus` union)
    - packages/apps/service-worker/src/server/editorRelease.ts (`getEditorHostStatus` → `resolveEditorRelease`; a missing `static/editor/current.json` yields `status: "unavailable"`, `reason: "not-installed"`)
    - packages/apps/service-worker/e2e/editor-host.spec.ts (the staged-release e2e flows and the raw SW-message registration helper)
    - packages/apps/service-worker/playwright.config.ts (the `withEditor` branch that chooses `build:with-editor` vs `build`)
    - packages/apps/editor/e2e/utils/projectSandbox.ts (the repo's only e2e helper-module shape)
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (section "packages/apps/service-worker/e2e/fixtures.ts")
    - .planning/phases/01-baseline-verification-net/01-RESEARCH.md (section "Pattern 4: Turn the silent skip into a required fixture")
  </read_first>
  <precondition>Plan 01-01 is complete; `pnpm --filter @motajs/service-worker build` succeeds and Playwright browsers are installed.</precondition>
  <action>
    Per D-16, create `packages/apps/service-worker/e2e/fixtures.ts` exporting a `test` extended from `@playwright/test`'s base test with a NAMED (not auto) fixture `editorRelease`. Do not make it auto: the first test in the spec asserts OPFS and preview behaviour that must keep working without an editor. The fixture must detect staging from a signal that exists on the app root, NOT from the `open-editor` test id: `open-editor` is rendered only by ProjectView on `service/:id/project/` (`MainView.tsx` lines 408, 421-432) and is gated on `editorStatus.data?.status === "ready"` (lines 246-248), so on `/` that element is absent whether or not a release is staged and a fixture that waits for it would throw unconditionally and fail the default staged run. Instead: navigate to `/`, then inside `page.evaluate` await `navigator.serviceWorker.ready` (awaiting `controllerchange` when the page is not yet controlled, exactly as `registerOpfsProject` does), take the controller, post `[requestId, "editor.status", null]` on it, and await the matching `[requestId, "editor.status", payload]` reply — the same `GetEditorHostStatusMessage` (`"editor.status"`, `@/idl`) that ProjectView's `useQuery(["editor-host"], …)` reads, so the fixture resolves exactly when the `open-editor` affordance can appear. Enforce a bounded wait (a timer that rejects) so an unresponsive service worker fails the fixture instead of hanging. When the reply is missing or its `status` is not `"ready"`, throw an `Error` whose message begins with `Editor release is not staged`, includes the reported `reason`/`message`, and names the exact remediation command `pnpm --filter @motajs/service-worker build:with-editor`. It must never call `test.skip`, and it must not let `MOTA_WITH_EDITOR=0` act as an escape hatch — a missing prerequisite is a failure. Record the observed `MOTA_WITH_EDITOR` value as a `testInfo.annotations` entry so the run configuration is visible in the report. Re-export `expect` from the module. Then convert `project-host.spec.ts` to import that `test` and `expect` instead of `@playwright/test`, request `editorRelease` in the `shows live Editor cache progress on the project page` test, and delete its `test.skip` line. For the conditional block in the first test, keep the editor assertions in the staged branch and, for the `MOTA_WITH_EDITOR=0` branch, push an explicit annotation stating that editor hosting is intentionally out of scope for this run and assert nothing about the editor instead of silently asserting less. Finally, change `packages/apps/service-worker/playwright.config.ts` `reuseExistingServer` from the constant true to `!process.env.CI`, mirroring the editor config, so a verification run can force a fresh server.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const spec=fs.readFileSync('packages/apps/service-worker/e2e/project-host.spec.ts','utf8');const fx=fs.readFileSync('packages/apps/service-worker/e2e/fixtures.ts','utf8');if(/test\.skip\s*\(/.test(spec)||/test\.skip\s*\(/.test(fx)){console.error('a runtime test.skip remains');process.exit(1)}if(!/editorRelease/.test(spec)){console.error('the editor-dependent test does not request the required fixture');process.exit(2)}if(!/editor\.status/.test(fx)){console.error('the fixture does not read the editor.status signal');process.exit(3)}if(!/Editor release is not staged/.test(fx)||!/build:with-editor/.test(fx)){console.error('the fixture does not throw the named remediation error');process.exit(4)}console.log('fixture contract present')"</automated>
    <fails_when>exit code 1 (a `test.skip(` call remains in the spec or the fixture), 2 (the editor-dependent test does not request the required fixture), 3 (the fixture does not probe `editor.status`), or 4 (the named `Editor release is not staged` / `build:with-editor` message is missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/apps/service-worker/e2e/project-host.spec.ts` contains no `test.skip(` call, and neither does `packages/apps/service-worker/e2e/fixtures.ts`.
    - The editor-dependent test requests the `editorRelease` fixture.
    - `packages/apps/service-worker/e2e/fixtures.ts` reads the service worker reply for the `editor.status` message and never depends on the `open-editor` test id being present on `/`.
    - `packages/apps/service-worker/e2e/fixtures.ts` throws an error whose message contains `Editor release is not staged` and the text `build:with-editor`, and contains no call to `test.skip` and no branch that lets the fixture resolve when the facilitator is absent.
    - The fixture is a named fixture, not `auto: true`.
    - `packages/apps/service-worker/playwright.config.ts` uses `reuseExistingServer: !process.env.CI`.
    - With the default staged configuration (`MOTA_WITH_EDITOR` unset, so `playwright.config.ts` runs `pnpm build:with-editor`), `pnpm --filter @motajs/service-worker exec playwright test e2e/project-host.spec.ts --grep "live Editor cache"` exits 0 — the fixture resolves and the test body runs. This is the positive (staged) polarity; Task 2's verifier asserts it automatically.
    - `pnpm --filter @motajs/service-worker exec tsc -b` still exits 0.
  </acceptance_criteria>
  <done>The silent skip is gone; the fixture resolves against a staged editor release and fails the test loudly with a named remediation message and an annotated run configuration only when the release is unstaged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a deterministic verifier that proves both fixture polarities</name>
  <files>scripts/verify/e2e-prerequisite.js</files>
  <read_first>
    - packages/apps/service-worker/e2e/fixtures.ts (the message this script asserts on)
    - packages/apps/service-worker/playwright.config.ts
    - packages/apps/service-worker/package.json (the test:e2e script and the build:with-editor script)
    - eslint.config.js
  </read_first>
  <precondition>Task 1 is complete; `packages/external/mota-js` is materialized (plan 01-01) and Playwright browsers are installed, because the positive polarity builds and serves the editor release; and no process is listening on 127.0.0.1:4178.</precondition>
  <action>
    Create `scripts/verify/e2e-prerequisite.js` as an ESM Node script (root `package.json` sets `"type": "module"`). It must prove BOTH polarities of the required fixture, in this order, because a verifier that only exercises the absent case cannot tell a conditional fixture from one that throws unconditionally.

    First the staged polarity: spawn `pnpm --filter @motajs/service-worker exec playwright test e2e/project-host.spec.ts --grep "live Editor cache"` with `CI` set to `1` and no `MOTA_WITH_EDITOR` override, so `playwright.config.ts`'s default branch runs `pnpm build:with-editor` and a real release is staged; capture stdout and stderr and require exit code 0. A non-zero exit means the fixture threw even though a release was present, or the editor-dependent test regressed — the script must print the child's combined output and fail, so the negative-polarity assertion below can never mask a fixture that fails unconditionally.

    Then the unstaged polarity: spawn the same Playwright target with `MOTA_WITH_EDITOR=0` and `CI=1`, which runs `pnpm build` (no `stage:editor`) and, because `vite build` empties `dist` and `CI=1` disables `reuseExistingServer`, always serves a freshly built, release-free tree on a fresh preview server. The child run must exit non-zero AND its combined output must contain the string `Editor release is not staged`. If that child exits zero, the script must fail with a message explaining that the silent-skip regression has returned. If it fails without the named message, the script must fail with the child's combined output so the real cause is visible. Exit 0 only when the staged polarity exited 0 and the unstaged polarity failed for the named reason. Keep the script lint-clean under the root flat config (double quotes, semicolons, two-space indent) and do not use a shell pipeline whose first stage could swallow a status.
  </action>
  <verify>
    <automated>node scripts/verify/e2e-prerequisite.js</automated>
    <fails_when>exit code is non-zero, which means the staged run did not pass (the fixture threw even though a release was staged), or the unstaged run reported success (the skip is back), or the unstaged run failed for a reason other than the named `Editor release is not staged` message</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify/e2e-prerequisite.js` exits 0.
    - The script runs the staged polarity first, with `CI=1` and no `MOTA_WITH_EDITOR` override, and requires exit code 0.
    - The script runs the unstaged polarity second, with `MOTA_WITH_EDITOR=0` and `CI=1`, and requires a non-zero exit whose combined output contains `Editor release is not staged`.
    - The script exits non-zero with an explanatory message when the unstaged child run exits 0.
    - The script exits non-zero and prints the child's combined output when the unstaged child fails for an unrecognised reason.
    - The script passes the root ESLint gate.
  </acceptance_criteria>
  <done>A single command proves both polarities: the fixture resolves against a freshly staged editor release (the editor-dependent test passes) and fails loudly with the named message when the release is unstaged.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the manifest protocol constants by driving the real artifact plugin</name>
  <files>packages/apps/editor/editor-artifact-plugin.test.ts</files>
  <read_first>
    - packages/apps/editor/editor-artifact-plugin.ts (the EditorArtifactManifest interface, validateEditorArtifact, and the closeBundle writer)
    - packages/apps/editor/editor-artifact-plugin.test.ts (the temp-dir `artifact` helper and `roots` cleanup)
    - packages/apps/editor/src/runtime/protocol.ts
    - packages/apps/editor/src/runtime/protocol.test.ts (the already-pinned runtime side)
  </read_first>
  <precondition>`packages/apps/editor/node_modules/blockly/media` and `packages/apps/editor/assets/theme` exist, because closeBundle copies both into the artifact directory.</precondition>
  <action>
    Extend `editor-artifact-plugin.test.ts` with a test that observes the real generated manifest rather than a source literal. Build a fabricated artifact directory containing an `index.html` and exactly one file matching `ts.worker-*.js` under `assets/`, because `validateEditorArtifact` requires exactly one standard ts worker and forbids css and html workers. Select the plugin named `mota-editor-artifact` from `editorArtifactPlugin("test")`, invoke its `configResolved` with a minimal config whose `build.outDir` is the fabricated root, await its `closeBundle`, then read and parse `editor-manifest.json` from that root. Assert `schemaVersion` equals 2, `environmentProtocolVersion` equals 1, `runtimeProtocolVersion` equals 3, and `entrypoints` equals the object `{ editor: "index.html", runtime: "runtime.html" }`. Add a comment stating that the manifest value 3 and the runtime constant 4 in `src/runtime/protocol.ts` are intentionally preserved for this milestone and must not be reconciled. Assert each value against its own literal; never assert a relation between 3 and 4. Leave `src/runtime/protocol.test.ts` untouched so the runtime side keeps its existing pin.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run editor-artifact-plugin.test.ts src/runtime/protocol.test.ts</automated>
    <fails_when>exit code is non-zero, or the new manifest test throws from validateEditorArtifact (the fabricated artifact does not satisfy the one-ts-worker rule), or vitest reports zero tests collected</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest run editor-artifact-plugin.test.ts src/runtime/protocol.test.ts` exits 0.
    - The new test asserts `runtimeProtocolVersion` equals 3, `schemaVersion` equals 2, and `environmentProtocolVersion` equals 1 on the manifest parsed from the plugin's written file.
    - The test asserts the entrypoints object literally.
    - The test contains a comment recording that the 3-versus-4 mismatch is intentionally preserved.
    - No assertion compares the manifest value to the runtime constant, and `git diff --exit-code -- packages/apps/editor/src/runtime/protocol.ts` exits 0.
  </acceptance_criteria>
  <done>The manifest side of the protocol constants is pinned by observing the plugin's real output, with both existing values left unchanged.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `packages/apps/service-worker/e2e/fixtures.ts` | required Playwright fixture module | VERIFY-06 |
| `scripts/verify/e2e-prerequisite.js` | two-polarity verifier (staged run passes, unstaged run fails loudly) | VERIFY-06 |
| `packages/apps/editor/editor-artifact-plugin.test.ts` (extended) | generated assertion | VERIFY-07 |
| `packages/apps/service-worker/playwright.config.ts` (modified) | config: deterministic server reuse | VERIFY-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI report → merge decision | A skipped check is indistinguishable from a passing check in the merge box, so a silent skip is a falsified gate. |
| Local verifier → child Playwright process | The verifier spawns a build-and-preview child that binds a localhost port. |
| Fabricated artifact → plugin validator | The test writes a synthetic build directory and drives the production plugin against it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Repudiation | silent e2e skip | high | mitigate | Remove the runtime `test.skip`; the required fixture reads the service worker's `editor.status` and throws a named error only when the release is unstaged, and a deterministic verifier asserts both polarities (staged run passes, unstaged run fails with the named message) so the skip cannot silently return and the fixture cannot be unconditional. |
| T-05-02 | Spoofing | verifier reusing an editor-staged preview server | medium | mitigate | The verifier sets `CI=1` on both children, and the config stops reusing an existing server under CI, so the unstaged run cannot observe a stale staged build left by the staged run. |
| T-05-03 | Tampering | protocol drift silently "fixed" | high | mitigate | The assertion pins the manifest value 3 and leaves `RUNTIME_PROTOCOL_VERSION = 4` untouched; no assertion relates the two values, and the intentional-preservation comment records the decision. |
| T-05-04 | Tampering | plugin test writing outside its temp directory | low | accept | The fabricated artifact is created with `fs.mkdtemp` under the OS temp directory and removed in `afterEach`, matching the existing helper in the same file. |
| T-05-SC | Tampering | test-only installs | low | accept | No package is added; `@playwright/test` and vitest are already pinned in the workspace. |

</threat_model>

<verification>
1. `node scripts/verify/e2e-prerequisite.js` exits 0 — it asserts that the staged (default) run of the editor-dependent test passes and that the unstaged (`MOTA_WITH_EDITOR=0`) run fails with `Editor release is not staged`.
2. `packages/apps/service-worker/e2e/project-host.spec.ts` contains no `test.skip(` call.
3. `pnpm --filter @motajs/editor exec vitest run editor-artifact-plugin.test.ts src/runtime/protocol.test.ts` exits 0.
4. `git diff --exit-code -- packages/apps/editor/src/runtime/protocol.ts packages/apps/editor/src/runtime/protocol.test.ts` exits 0.
</verification>

<success_criteria>
- No e2e test in the workspace silently skips itself; the editor-dependent service-worker test passes when the editor release is staged and fails loudly with a named remediation command only when it is missing.
- Both protocol constants are pinned by tests, each against its own literal, with neither value changed.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-05-SUMMARY.md` when done.
</output>
