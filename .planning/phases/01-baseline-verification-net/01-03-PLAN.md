---
phase: 01-baseline-verification-net
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - packages/apps/editor/src/fs/__tests__/persistExecutor.invariants.test.ts
  - packages/apps/editor/src/fs/__tests__/persistenceMonitor.invariants.test.ts
  - packages/apps/editor/src/fs/__tests__/persistNoRollback.invariants.test.ts
autonomous: true
requirements:
  - VERIFY-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A persistence failure followed by a successful retry ends in the idle state, and the retry re-submits the retained failed intent."
    - "Concurrent intents are latest-wins: at most one intent executes and at most one newer intent is retained, so a superseded pending intent is never executed."
    - "A failure stays invisible as an error while a newer intent is pending, and only surfaces as the error state when nothing newer remains."
    - "`flush()` rejects on a persisted failure while `whenQuiescent()` never rejects, and the monitor's flush aggregate carries the `工程文件写入失败` message."
    - "A persistence failure never rolls editor state back: the in-memory resource value keeps the new value while the on-disk file keeps the old one, and the failure is reported through the resource's persist status."
  artifacts:
    - "packages/apps/editor/src/fs/__tests__/persistExecutor.invariants.test.ts"
    - "packages/apps/editor/src/fs/__tests__/persistenceMonitor.invariants.test.ts"
    - "packages/apps/editor/src/fs/__tests__/persistNoRollback.invariants.test.ts"
  key_links:
    - "the new suites observe through `new PersistExecutor()` / `new PersistenceMonitor()` so they survive the later singleton removal (KERN-01)"
    - "fault injection goes through test/utils/MemoryFileSystem, the repo's canonical I/O double, never through a mocked fs facade"
    - "the no-rollback suite drives the real resource path via test/utils/sampleProject loadSampleProject()"
---

<objective>
Freeze the persistence invariants that the milestone must not change, in new independent characterization suites that sit beside — and never modify — the existing tests.

Purpose: Phase 4 relocates `src/fs/*` into `editor-core`. D-10 requires these suites to be co-located in `__tests__/`. D-09/D-12 make the safety net explicit: the existing suites stay untouched so a refactor cannot quietly rewrite the assertions it is supposed to satisfy, and a red characterization test means a regression, not a stale expectation. D-11 scopes this plan to the invariants the requirement names, not to a full state-transition recording. This plan depends on plan 01-02 so the recorded baseline count describes the pre-Phase-1 editor unit suite; adding the characterization suites afterwards is a deliberate, visible delta rather than a change that silently inflates the "before" numbers.
Output: three co-located `*.invariants.test.ts` suites covering error→retry→idle, concurrent latest-wins, the failure-visibility rule, flush-versus-quiescent behaviour, and the no-UI-rollback contract.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-RESEARCH.md
@.planning/phases/01-baseline-verification-net/01-PATTERNS.md
@.planning/codebase/TESTING.md
@packages/apps/editor/src/fs/PersistExecutor.ts
@packages/apps/editor/src/fs/PersistenceMonitor.ts
@packages/apps/editor/test/utils/MemoryFileSystem.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the PersistExecutor state machine (error to retry to idle, latest-wins)</name>
  <files>packages/apps/editor/src/fs/__tests__/persistExecutor.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/fs/PersistExecutor.ts
    - packages/apps/editor/src/fs/__tests__/PersistExecutor.test.ts (the untouched analog for import and scheduling idioms)
    - packages/apps/editor/test/utils/testHelpers.ts
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (section "persistExecutor.invariants.test.ts")
    - .planning/phases/01-baseline-verification-net/01-RESEARCH.md (section "Pattern 3: Characterization suites that pin invariants, not structure")
  </read_first>
  <precondition>Plan 01-01 is complete and `pnpm --filter @motajs/editor exec vitest --version` prints a 4.x version.</precondition>
  <action>
    Create `src/fs/__tests__/persistExecutor.invariants.test.ts` beside the existing `PersistExecutor.test.ts`, importing `PersistExecutor` from `../PersistExecutor` and helpers from `@test/utils/testHelpers` and `alien-signals`. Freeze only these behaviours: (1) a failing intent leaves the status in the error state, `retry()` re-submits the retained failed intent, and a successful retry returns the status to idle with the failure cleared; (2) `retry()` is a no-op while the status is not error; (3) concurrent latest-wins — schedule a slow intent, then schedule two more before the queue drains, and assert the slow one completed plus exactly one newest intent executed while the middle one was discarded; (4) a failure that coincides with a newer pending intent does not surface as the error state, because the loop continues into the newer intent; (5) `hasPending()` is true while executing or while an intent is pending and false once quiescent; (6) `flush()` rejects with the stored error after a terminal failure while `whenQuiescent()` resolves. Use `new PersistExecutor()` throughout — never the module-level singleton shape — so the suite keeps passing when Phase 3 replaces the singletons. Follow the observation-first discipline: for each assertion, first call the unit with the intended input and assert a deliberately wrong value, run it, read the real value out of the failure, then write the correct expectation. Do not derive expectations by reading `PersistExecutor.ts`, and do not assert internal fields or record a full state-transition snapshot.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistExecutor.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or vitest reports zero tests collected, or any test in the file is skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistExecutor.invariants.test.ts` exits 0 with every test passed and none skipped.
    - The file contains tests naming each of: retry-after-error returning to idle, latest-wins with a discarded middle intent, a failure hidden behind a newer pending intent, `hasPending()` transitions, and `flush()` rejecting while `whenQuiescent()` resolves.
    - The file imports `PersistExecutor` from `../PersistExecutor` and instantiates it directly; it does not import any module-level singleton.
    - `git diff --exit-code -- packages/apps/editor/src/fs/__tests__/PersistExecutor.test.ts` exits 0 (the existing suite is untouched).
    - Breaking one behaviour under test (for example making `retry()` ignore the failed intent) makes at least one assertion in this file go red; revert that deliberate break before committing.
  </acceptance_criteria>
  <done>A co-located characterization suite pins the PersistExecutor error→retry→idle and latest-wins invariants, can be seen to fail against a deliberate break, and leaves the existing suite untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the PersistenceMonitor path ownership, failure visibility and flush contract</name>
  <files>packages/apps/editor/src/fs/__tests__/persistenceMonitor.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/fs/PersistenceMonitor.ts
    - packages/apps/editor/src/fs/__tests__/PersistenceMonitor.test.ts (the untouched analog)
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (section "persistenceMonitor.invariants.test.ts")
  </read_first>
  <precondition>Task 1 is complete (the executor invariants are already frozen, so a monitor failure can be attributed to the monitor layer).</precondition>
  <action>
    Create `src/fs/__tests__/persistenceMonitor.invariants.test.ts`. Use `new PersistenceMonitor()` in `beforeEach`, never the exported `persistenceMonitor` singleton, so the suite survives the Phase 3 singleton removal. Freeze only these behaviours: (1) path normalization — a schedule against `./project\\data.js` and a later schedule against `project/data.js` land on one controller and execute in submission order; (2) a failure is retained in the failed set and is cleared only after a genuinely successful execution; (3) an existing failure stays visible while its retry is in progress; (4) `retryFailed()` returns the still-failing paths and leaves the recovered ones out of the aggregate; (5) `flush()` rejects with an `AggregateError` whose message is `工程文件写入失败` when any listed path has failed, and resolves when it has not; (6) `whenQuiescent()` resolves even when a failure is present; (7) `statusFor` precedence is error over persisting over idle. Use `wait` from `@test/utils/testHelpers`. Follow the observation-first discipline for every expectation and do not assert private fields.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistenceMonitor.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or vitest reports zero tests collected, or any test in the file is skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistenceMonitor.invariants.test.ts` exits 0 with every test passed and none skipped.
    - The file asserts the normalized-path single-controller behaviour, the retained-then-cleared failure, the failure-visible-during-retry rule, the partial `retryFailed()` aggregate, the `flush()` rejection message `工程文件写入失败`, and the non-rejecting `whenQuiescent()`.
    - The file instantiates `new PersistenceMonitor()` and never imports the `persistenceMonitor` singleton.
    - `git diff --exit-code -- packages/apps/editor/src/fs/__tests__/PersistenceMonitor.test.ts` exits 0.
    - This file contains no `loadSampleProject` import, so it needs no mota-js submodule to run.
  </acceptance_criteria>
  <done>A co-located characterization suite pins the monitor's path ownership, failure visibility, retry aggregation, and flush-versus-quiescent contract without touching the existing suite.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the no-rollback contract on a real resource</name>
  <files>packages/apps/editor/src/fs/__tests__/persistNoRollback.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/project/data/__tests__/persistStatus.integration.test.ts
    - packages/apps/editor/test/utils/sampleProject.ts
    - packages/apps/editor/test/utils/MemoryFileSystem.ts
    - packages/apps/editor/src/project/commands/tableCommands.ts
    - packages/apps/editor/src/fs/PersistenceMonitor.ts
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (section "persistenceMonitor.invariants.test.ts", the fault-injection block)
  </read_first>
  <precondition>Plan 01-01 is complete so `MOTA_JS_ROOT/project` is readable; `test/utils/MemoryFileSystem` exposes `setWriteError`, `setWriteErrorForPath`, `clearWriteError`, and `clearWriteErrorForPath`.</precondition>
  <action>
    Create `src/fs/__tests__/persistNoRollback.invariants.test.ts` as the one deliberately submodule-coupled file in this set, kept separate so the other two suites stay mota-js-free. Drive the real resource path with `loadSampleProject()` from `@test/utils/sampleProject`, and inject faults through the returned `MemoryFileSystem` rather than mocking an fs facade. Freeze the contract stated verbatim in `PersistExecutor`'s module documentation — persistence failures never roll editor state back — with two observable assertions: after a write failure the resource's in-memory value still equals the newly written value, and the on-disk content still equals the pre-edit value; and the failure is reported on the resource's persist status and on the monitor's failed set. Then clear the injected failure, retry, and assert the status returns to idle and the disk content becomes the new value. Reset shared singletons in `afterEach` exactly as the existing integration suite does (`FileHandlerManager.clear()`, `projectData.resetForTests()`, plus clearing the injected write errors and the write delay). Do not reuse or modify `persistStatus.integration.test.ts`; keep the assertions to the no-rollback invariant and its recovery, not to status-timing details already covered elsewhere.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistNoRollback.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or vitest reports zero tests collected, or the run fails at collection with a mota-js root error (which would mean plan 01-01's submodule is not in place)</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest run src/fs/__tests__/persistNoRollback.invariants.test.ts` exits 0 with every test passed.
    - The suite asserts both halves of the no-rollback contract: the in-memory value keeps the new value while the on-disk content keeps the old one.
    - The suite asserts recovery: after clearing the injected failure and retrying, the persist status returns to idle and the disk content becomes the new value.
    - Fault injection uses `MemoryFileSystem` methods, not `vi.mock` of the fs facade.
    - `afterEach` clears `FileHandlerManager`, calls `projectData.resetForTests()`, and clears injected write errors and write delay.
    - `git diff --exit-code -- packages/apps/editor/src/project/data/__tests__/persistStatus.integration.test.ts` exits 0.
  </acceptance_criteria>
  <done>The no-UI-rollback contract is frozen against a real resource and the in-memory-versus-disk divergence is asserted in both directions.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `src/fs/__tests__/persistExecutor.invariants.test.ts` | characterization suite | VERIFY-03 |
| `src/fs/__tests__/persistenceMonitor.invariants.test.ts` | characterization suite | VERIFY-03 |
| `src/fs/__tests__/persistNoRollback.invariants.test.ts` | characterization suite | VERIFY-03 |

No production source file is modified: D-09 requires the existing suites to stay untouched so the refactor cannot rewrite its own safety net.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → production invariants | A wrong or over-fitted assertion silently becomes the definition of correct behavior for every later phase. |
| Test fixture → on-disk sample project | The no-rollback suite reads real mota-js files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | characterization assertions | high | mitigate | Observation-first derivation (assert a deliberately wrong value, read the real one from the failure) prevents assertions derived from reading intent; the D-12 rule that red means fix the implementation, never the assertion, keeps the net honest. |
| T-03-02 | Repudiation | a suite that cannot fail | high | mitigate | Each new suite must be seen red against one deliberate implementation break before it is accepted as a safety net; the break is reverted and never committed. |
| T-03-03 | Information Disclosure | fixture paths and content in failure output | low | accept | The sample project is public mota-js content; no secret or host path is involved. |
| T-03-SC | Tampering | test-only installs | low | accept | No package is added; vitest and the existing fixtures are reused. |

</threat_model>

<verification>
1. All three files run green under the editor vitest configuration.
2. `git diff` against the three pre-existing neighbor suites is empty.
3. Each suite has been observed red against one deliberate implementation break, then green after the revert.
4. The two non-fixture suites contain no `loadSampleProject` import.
</verification>

<success_criteria>
- The five persistence invariants named by VERIFY-03 are frozen by executable tests.
- A regression in `PersistExecutor` or `PersistenceMonitor` turns at least one assertion red.
- The existing persistence suites are unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-03-SUMMARY.md` when done.
</output>
