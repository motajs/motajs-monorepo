---
phase: 01-baseline-verification-net
plan: 06
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - packages/apps/editor/vitest.config.ts
  - packages/apps/editor/vite.config.ts
  - packages/apps/editor/package.json
  - package.json
  - packages/libs/file2x/package.json
  - packages/libs/packer/package.json
  - packages/libs/react-dark-mode/package.json
  - packages/libs/react-hooks/package.json
  - packages/libs/react-monaco-editor/package.json
  - packages/libs/react-store/package.json
  - packages/libs/utils/package.json
  - packages/apps/editor/eslint.config.js
  - .planning/baseline/BASELINE.md
autonomous: true
requirements:
  - VERIFY-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The editor's test configuration lives in its own `vitest.config.ts` that never imports the mota-js root resolver, while `vite.config.ts` continues to serve dev and build."
    - "The editor unit suite still collects the tests that live outside `src` (the twelve `test/blockly/*.test.ts` files and the two `test/mapEditor/*.test.ts` files), proving no `include` restriction was introduced."
    - "Every workspace package that owns a tsconfig and a meaningful source tree exposes a `typecheck` script, and a root fan-out runs them all."
    - "A single non-fixing root `lint` invocation is green, and root fan-out scripts exist for test, typecheck and build using `pnpm -r run`."
  artifacts:
    - "packages/apps/editor/vitest.config.ts"
    - "`typecheck` scripts in package.json for the editor plus seven libraries"
    - "root fan-out scripts (lint, lint:fix, test, typecheck, build) in package.json"
  key_links:
    - "the editor vitest config keeps the `@`, `@test` and `@styled-system` aliases and the jsdom/setupFiles/exclude settings that the previous shared test block provided"
    - "`pnpm -r run <script>` skips packages that lack the script, so absent scripts in @motajs/config and @motajs/react-dark-mode are harmless"
    - "the CI lint gate will call the root lint script, so it must not use `--fix`"
---

<objective>
Land the package-script and test-config surface that VERIFY-02's CI gate will call: an editor-owned vitest config decoupled from the mota-js root, per-package `typecheck` scripts, and root fan-out scripts with a non-fixing lint.

Purpose: today the editor is the only workspace package that co-locates its test block with dev/build configuration, so a missing mota-js submodule throws at config load; only `@motajs/service-worker` has a `typecheck` script; and the root `lint` script is `eslint --fix`, which as a gate can never fail on formatting and silently rewrites the checkout. This plan closes all three gaps before the CI workflow is written.
Output: `packages/apps/editor/vitest.config.ts`, `typecheck` scripts across the workspace, and root fan-out scripts including `lint: eslint .`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-RESEARCH.md
@.planning/phases/01-baseline-verification-net/01-PATTERNS.md
@.planning/baseline/baseline.json
@.planning/baseline/BASELINE.md
@packages/apps/editor/vite.config.ts
@packages/apps/service-worker/vitest.config.ts
@packages/apps/service-worker/package.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Split the editor vitest config out of the Vite config without changing collection</name>
  <files>packages/apps/editor/vitest.config.ts, packages/apps/editor/vite.config.ts</files>
  <read_first>
    - packages/apps/editor/vite.config.ts (the `test` block at lines 51-56 and the configDefaults import at line 3)
    - packages/apps/service-worker/vitest.config.ts (the per-package model to copy)
    - packages/apps/editor/tsconfig.app.json (the path aliases the test config must mirror)
    - packages/apps/editor/test/setup.ts
    - packages/apps/editor/mota-root.ts
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (the resulting-shape block for this file)
  </read_first>
  <precondition>Plan 01-02 recorded `.planning/baseline/baseline.json` with a `unit.editor.total`, and `pnpm --filter @motajs/editor test` currently exits 0.</precondition>
  <action>
    Create `packages/apps/editor/vitest.config.ts` modelled on `packages/apps/service-worker/vitest.config.ts`, with three deliberate differences. First, the alias map must contain all three editor aliases from `vite.config.ts`: `@` to `src`, `@test` to `test`, and `@styled-system` to `styled-system`, each resolved with `import.meta.dirname` so the file is path-independent. Second, the test block must keep `environment: "jsdom"`, `globals: true`, `setupFiles: ["./test/setup.ts"]`, and `exclude: [...configDefaults.exclude, "e2e/**"]` with `configDefaults` imported from `vitest/config`. Third, and critically, the config must NOT set an `include` key: the editor currently relies on Vitest's default include, which is what collects `test/blockly/*.test.ts` and `test/mapEditor/*.test.ts` from outside `src`; adding an `include` would silently drop fourteen test files and invalidate the baseline. Above all, the new config must not import `./mota-root` or `MOTA_JS_ROOT` — that import is the load-time throw the split exists to remove. Then delete the `test` block from `vite.config.ts` and drop `configDefaults` from its `vitest/config` import, keeping `defineConfig` from the same module and keeping the `MOTA_JS_ROOT` import because `publicDir` and `motaServerPlugin` still need it for dev and build.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor test -- --reporter=json --outputFile=%TEMP%\editor-unit-after-split.json</automated>
    <fails_when>exit code is non-zero, or `packages/apps/editor/vitest.config.ts` mentions `MOTA_JS_ROOT` or declares an `include` key, or the JSON report omits `test/blockly/uiRoundTrip.test.ts` or `test/mapEditor/materialLayout.test.ts`</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/apps/editor/vitest.config.ts` exists and contains no occurrence of `mota-root` or `MOTA_JS_ROOT`.
    - The new config declares the three aliases `@`, `@test`, `@styled-system`, and does not declare an `include` key.
    - `packages/apps/editor/vite.config.ts` no longer contains a `test:` block, still imports `MOTA_JS_ROOT`, and no longer imports `configDefaults` (which would trip `noUnusedLocals`).
    - The JSON report written by the verify command lists a test file whose path ends with `test/blockly/uiRoundTrip.test.ts` and another ending with `test/mapEditor/materialLayout.test.ts`.
    - `pnpm --filter @motajs/editor test` exits 0 and its total test count is greater than or equal to the `unit.editor.total` recorded in `.planning/baseline/baseline.json`.
    - `pnpm --filter @motajs/editor exec tsc -b` still exits 0 (the node tsconfig includes `vite.config.ts`).
  </acceptance_criteria>
  <done>The editor owns a submodule-free vitest config, the Vite config serves dev and build only, and collection of the out-of-src test files is proven unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add a per-package typecheck script across the workspace</name>
  <files>packages/apps/editor/package.json, packages/libs/file2x/package.json, packages/libs/packer/package.json, packages/libs/react-dark-mode/package.json, packages/libs/react-hooks/package.json, packages/libs/react-monaco-editor/package.json, packages/libs/react-store/package.json, packages/libs/utils/package.json, .planning/baseline/BASELINE.md</files>
  <read_first>
    - packages/apps/service-worker/package.json (the existing `typecheck` script shape)
    - packages/apps/editor/tsconfig.json, packages/apps/editor/tsconfig.app.json, packages/apps/editor/tsconfig.node.json
    - packages/libs/config/tsconfig.lib.base.json
    - packages/libs/utils/tsconfig.json and packages/libs/file2x/tsconfig.json (single-project configs, no composite)
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (the `typecheck` additions block, including the `tsc -b` fallback note)
  </read_first>
  <precondition>Task 1 is complete and `pnpm --filter @motajs/editor build` still resolves `styled-system` (the editor's typecheck program includes `src`, which imports it).</precondition>
  <action>
    Add a `typecheck` script to `packages/apps/editor/package.json` and to each library that owns a `tsconfig.json` and is currently missing one: `@motajs/file2x`, `@motajs/packer`, `@motajs/react-dark-mode`, `@motajs/react-hooks`, `@motajs/react-monaco-editor`, `@motajs/react-store`, `@motajs/utils`. The editor uses `tsc -b`, matching `@motajs/service-worker`, because its `tsconfig.json` is a solution file referencing two project files. For the libraries, prefer `tsc -b` as well; each library's `tsconfig.json` is a single project that extends `@motajs/config/tsconfig.lib.base.json`, which sets `noEmit: true` and no `composite`, and TypeScript's build mode may reject a project it cannot emit. If a library's `tsc -b` invocation errors on the build/emit combination, set that package's script to `tsc --noEmit -p tsconfig.json` instead and record in `.planning/baseline/BASELINE.md` which form each package uses. Do not add a `typecheck` script to `@motajs/config` or `@motajs/theme`: `@motajs/config` is configuration-only, and `@motajs/theme` has no discoverable package manifest. Do not add any dependency; `typescript` is already a workspace dev dependency. Keep every package.json edit to a single added script line so the diff stays reviewable.
  </action>
  <verify>
    <automated>pnpm -r run typecheck</automated>
    <fails_when>exit code is non-zero, or the run reports that no package had the script, or a package's typecheck reports a TypeScript error</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm -r run typecheck` exits 0.
    - `packages/apps/editor/package.json` declares `"typecheck": "tsc -b"`.
    - Each of `@motajs/file2x`, `@motajs/packer`, `@motajs/react-dark-mode`, `@motajs/react-hooks`, `@motajs/react-monaco-editor`, `@motajs/react-store` and `@motajs/utils` declares a `typecheck` script.
    - `.planning/baseline/BASELINE.md` records which packages use `tsc -b` and which use `tsc --noEmit -p tsconfig.json`, if any differ.
    - No dependency is added or changed in any manifest; `git diff --exit-code -- pnpm-lock.yaml` exits 0.
  </acceptance_criteria>
  <done>Every workspace package with a tsconfig and a source tree can be typechecked, and `pnpm -r run typecheck` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Add root fan-out scripts with a non-fixing lint gate</name>
  <files>package.json, packages/apps/editor/eslint.config.js</files>
  <read_first>
    - package.json (the current scripts block, which contains only `lint: eslint --fix`)
    - eslint.config.js
    - packages/apps/editor/eslint.config.js
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (the root fan-out block and the "Anti-Patterns" note on `lint --fix`)
  </read_first>
  <precondition>Plan 01-01 repaired the install so `pnpm exec eslint` resolves; Tasks 1 and 2 are complete so the fan-out has a `typecheck` script to run.</precondition>
  <action>
    Add root scripts to `package.json`: `"lint": "eslint ."` (replacing the current `eslint --fix`, because a fixing gate can never fail on formatting), `"lint:fix": "eslint --fix"` (preserving the developer convenience under a distinct name), `"test": "pnpm -r run test"`, `"typecheck": "pnpm -r run typecheck"`, and `"build": "pnpm -r run build"`. Use `pnpm -r run`, which skips packages that lack the script, rather than `pnpm -r exec`, which does not. ESLint 9 determines configuration per target file by searching from that file's directory upward, so one root invocation applies `packages/apps/editor/eslint.config.js` to editor files and the root config elsewhere; confirm this by printing the effective config for an editor source file. If the editor's flat config leaves a file under `packages/apps/editor/` unconfigured (its `files` pattern currently covers only `.ts` and `.tsx`, while the directory also contains `postcss.config.cjs`), extend that editor config's `files` list to cover the JS-family extensions with the same base rules rather than relaxing any rule or adding an ignore. Do not add a `paths:` filter or any CI concept to scripts.
  </action>
  <verify>
    <automated>pnpm lint</automated>
    <fails_when>exit code is non-zero, or ESLint reports an error about a file having no matching configuration, or ESLint reports a lint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0 and the root script is `eslint .` with no `--fix`.
    - `pnpm lint:fix` exists and maps to `eslint --fix`.
    - The root `package.json` declares `test`, `typecheck` and `build` fan-outs that each use `pnpm -r run`.
    - `pnpm typecheck` exits 0.
    - `pnpm test` exits 0.
    - Printing the effective ESLint config for a file under `packages/apps/editor/src` shows the editor-local rules (including `prefer-arrow-callback`) rather than the root stylistic rule set.
    - No file under `packages/apps/editor/` is reported as having no matching configuration.
  </acceptance_criteria>
  <done>A single root invocation each for lint, test, typecheck and build works, and the lint gate is non-fixing and green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `packages/apps/editor/vitest.config.ts` | per-package test config | VERIFY-02 |
| `typecheck` scripts in 8 package manifests | script surface | VERIFY-02 |
| root `lint`/`lint:fix`/`test`/`typecheck`/`build` scripts | script surface | VERIFY-02 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Root script → every workspace package | `pnpm -r run` executes arbitrary package scripts with the developer's (and CI's) privileges. |
| Test config → fixture resolution | The vitest config decides which files are collected and where imports resolve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Repudiation | fixing lint gate | high | mitigate | The root lint script becomes `eslint .`; `--fix` moves to a distinctly named `lint:fix`, so a gate run can no longer rewrite the checkout and pass. |
| T-06-02 | Tampering | a restrictive `include` silently dropping tests | high | mitigate | The new vitest config deliberately omits `include`, and Task 1 verifies from the runner's own report that the `test/blockly` and `test/mapEditor` suites are still collected, so a dropped-file regression cannot pass unnoticed. |
| T-06-03 | Elevation of Privilege | unconfigured-file ESLint workaround | low | accept | If an editor file were unconfigured, the fix is to widen the editor config's `files` patterns, never to broaden ignores or disable rules; the acceptance criteria require the editor-local rules to remain effective. |
| T-06-SC | Tampering | package installs (supply chain) | low | accept | No package is added; only script entries and one config file change. |

</threat_model>

<verification>
1. `pnpm lint`, `pnpm test`, `pnpm typecheck` and `pnpm build` all exit 0 from the root.
2. `packages/apps/editor/vitest.config.ts` contains no `MOTA_JS_ROOT` reference and no `include` key.
3. The editor unit JSON report still contains the out-of-src `test/blockly` and `test/mapEditor` suites.
4. `git diff --exit-code -- pnpm-lock.yaml` exits 0.
</verification>

<success_criteria>
- The four CI gate commands exist as root scripts and are green locally.
- The editor's unit tests no longer depend on config-load evaluation of the mota-js root, while still collecting the full existing suite.
- The lint gate is non-fixing.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-06-SUMMARY.md` when done.
</output>
