---
phase: 01-baseline-verification-net
plan: 01
subsystem: infra
tags: [pnpm, git-submodule, vitest, environment-repair, proxy]

# Dependency graph
requires: []
provides:
  - "`packages/external/mota-js` materialized at gitlink SHA 3efb548e407ad2b8007b498cb98401e2012a0b55"
  - "a resolvable workspace `node_modules` tree (editor tooling starts)"
  - "`packages/apps/editor/styled-system/` regenerated by the editor `prepare` script"
affects: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, phase-04-resource-edit-move, editor-build]

actuals:
  tokens: 5000
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns:
    - "proxy + SSH→HTTPS rewrite for submodule fetch: `HTTP_PROXY`/`HTTPS_PROXY` env plus `git -c http.proxy=… -c url.https://github.com/.insteadOf=git@github.com:` (session-scoped, nothing persisted)"
    - "direct submodule fetch when `git submodule update --init` cannot resolve the recorded revision"

key-files:
  created: []
  modified: []

key-decisions:
  - "Ran in the main working tree, not a worktree: environment repair and submodule materialization are working-tree operations that isolation would have hidden."
  - "Records that local pnpm is 10.15.0 while CI pins 11.10.0; the install-ladder and `--frozen-lockfile` both succeed regardless, so the local/CI major gap is a documented fact, not a blocker."
  - "Records that the upstream submodule fetch required an explicit proxy — a bare `git submodule update --init` could not resolve the recorded revision in this network environment."

patterns-established:
  - "Submodule recovery when the recorded revision is not local: fetch inside `.git/modules/...` with proxy + SSH→HTTPS rewrite, then `git checkout --force <sha>`."

requirements-completed: [VERIFY-01]

coverage:
  - id: D1
    description: "Wave 0 exit criterion — a per-package unit-test command starts and runs to completion in this checkout, and the mota-js submodule is materialized at the pinned SHA"
    requirement: VERIFY-01
    verification:
      - kind: unit
        ref: "pnpm --filter @motajs/editor test → 92 test files passed, 865 tests passed, 0 failed (82.6s)"
        status: pass
      - kind: integration
        ref: "git submodule status packages/external/mota-js → ' 3efb548e407ad2b8007b498cb98401e2012a0b55 (v2.10.3-release-2-g3efb548e)'"
        status: pass
      - kind: integration
        ref: "pnpm install --frozen-lockfile → exit 0; git diff --exit-code -- pnpm-lock.yaml → exit 0"
        status: pass
    human_judgment: false

duration: 85min
completed: 2026-09-20
status: complete
---

# Phase 1: Baseline & Verification Net — Plan 01 Summary

**Repaired this checkout's broken pnpm workspace, materialized the mota-js submodule at its pinned SHA, and got the editor unit suite to actually run: 92 files / 865 tests green.**

## Performance

- **Duration:** ~85 min (includes a failed submodule fetch and a network-proxy retry)
- **Completed:** 2026-09-20T08:11:41Z
- **Tasks:** 2
- **Files modified:** 0 tracked source files (working-tree state only)

## Accomplishments
- Repaired the workspace dependency tree so `pnpm --filter @motajs/editor exec vitest --version` resolves (`vitest/4.0.18`), with `pnpm-lock.yaml` and `allowBuilds` untouched.
- Materialized `packages/external/mota-js` at `3efb548e407ad2b8007b498cb98401e2012a0b55`; the submodule adds no pnpm workspace project (no `package.json`).
- Ran the editor unit suite to completion for the first time in this checkout: **92 test files passed, 865 tests passed, 0 failed** (82.59s); zero tests reported skipped.
- Confirmed `packages/apps/editor/styled-system/` exists after the editor `prepare` (`panda codegen`) step.

## Task Commits

No task commit was required: this plan modifies **no tracked source file**. Its outcomes are working-tree state (a repaired `node_modules`, a materialized submodule gitlink whose recorded SHA did not change), so there is no atomic per-task commit to make.

**Plan metadata:** committed with this SUMMARY.md (docs: complete plan 01-01)

_Note: `git status` before this commit showed only `.planning/STATE.md` (orchestrator-owned) and untracked GSD runtime files._

## Files Created/Modified
None (tracked). Working-tree state changed:
- `packages/external/mota-js/` — populated at the pinned SHA
- `node_modules/` (root + `packages/apps/editor/node_modules/`) — repaired
- `packages/apps/editor/styled-system/` — regenerated (gitignored)

## Decisions Made
- Executed on the **main working tree**, not worktree-isolated — environment repair and submodule materialization cannot be done inside an isolated worktree.
- Documented the local ↔ CI pnpm major gap (local `10.15.0`, CI `11.10.0`) as a fact rather than a blocker; both the repair ladder and `--frozen-lockfile` succeed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Submodule fetch required an explicit proxy and a direct fetch path**
- **Found during:** Task 2 (submodule initialization)
- **Issue:** `git submodule update --init packages/external/mota-js` failed with `fatal: Unable to find current revision in submodule path`; the submodule dir contained only `.git`, and the clone under `.git/modules/packages/external/mota-js` had no resolvable revision. The upstream remote is the SSH URL `git@github.com:ckcz123/mota-js.git`.
- **Fix:** With the user's direction, set `HTTP_PROXY`/`HTTPS_PROXY` to `http://127.0.0.1:7890` and added a session-scoped `git -c url.https://github.com/.insteadOf=git@github.com:` rewrite, then fetched directly inside the submodule (`git fetch --tags --prune origin`) and checked out the pinned SHA with `git checkout --force 3efb548e407ad2b8007b498cb98401e2012a0b55`. Nothing persisted: `.gitmodules` and the repo config are unchanged.
- **Files modified:** none tracked (`.gitmodules` verified unchanged, exit 0)
- **Verification:** `git submodule status` shows a leading space + the pinned SHA; `index.html`, `main.js`, `project/data.js`, `_server/table/data.comment.js` all exist; no `package.json` at the submodule root.
- **Committed in:** n/a (working-tree state; no tracked change)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to complete the plan in this network environment; no scope creep, no dependency change, no lockfile drift.

## Issues Encountered
- The submodule fetch initially failed and required a proxy plus a direct fetch-then-checkout path (see Deviation 1). Resolved.
- Local pnpm is `10.15.0` while CI pins `11.10.0`; the repair and `--frozen-lockfile` both succeed, so no action was taken (changing the pnpm major would be a scope change).
- Node is `v22.18.0` locally; CI uses Node 24. Noted for the baseline record, not changed here.

## User Setup Required
None — no external service configuration required. (The proxy used for the submodule fetch was session-scoped and is not persisted.)

## Next Phase Readiness
- Ready for plan 01-02 (quantified baseline): the tree can now execute, and the submodule-reading unit fixtures load real mota-js files.
- Verbatim verify evidence for the baseline record:
  - `pnpm --filter @motajs/editor test` → 92 files / 865 tests passed / 0 failed / 82.59s
  - `git submodule status packages/external/mota-js` → ` 3efb548e407ad2b8007b498cb98401e2012a0b55 (v2.10.3-release-2-g3efb548e)`
  - `pnpm install --frozen-lockfile` → exit 0; `pnpm-lock.yaml` unchanged; `.gitmodules` unchanged
  - `pnpm --filter @motajs/editor exec vitest --version` → `vitest/4.0.18 win32-x64 node-v22.18.0`
- No blockers.

---
*Phase: 01-baseline-verification-net*
*Completed: 2026-09-20*
