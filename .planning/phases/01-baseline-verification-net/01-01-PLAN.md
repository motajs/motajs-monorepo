---
phase: 01-baseline-verification-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - VERIFY-01
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A per-package unit-test command starts and runs to completion in this checkout — no `Cannot find module` from dangling workspace junctions, no `UNKNOWN: unknown error` from the install."
    - "`packages/external/mota-js` is materialized at the recorded gitlink SHA 3efb548e407ad2b8007b498cb98401e2012a0b55, so every fixture that reads real mota-js files can load."
    - "`pnpm-lock.yaml` is byte-identical across the repair, proving the submodule adds no pnpm workspace project."
  artifacts:
    - "packages/external/mota-js — gitlink materialized at 3efb548e407ad2b8007b498cb98401e2012a0b55"
    - "packages/apps/editor/styled-system — regenerated by the editor prepare script"
  key_links:
    - "packages/apps/editor/mota-root.ts REQUIRED_PATHS (index.html, main.js, project/data.js, _server/table/data.comment.js) resolve against the initialized submodule"
    - "packages/apps/editor/test/utils/sampleProject.ts walks MOTA_JS_ROOT/project at unit-test runtime"
---

<objective>
Repair the local verification environment and materialize the `packages/external/mota-js` submodule so every later Phase 1 measurement is taken on a tree that can actually execute.

Purpose: RESEARCH measured this working copy as unable to run a single test or build — every pnpm workspace junction under `packages/apps/editor/node_modules` is dangling, `pnpm install --frozen-lockfile` aborts with `UNKNOWN: unknown error`, and the submodule directory is empty (confirmed again this session: invoking the store's eslint fails with `Cannot find module 'debug'`). This plan is the phase's Wave 0 blocking prerequisite. No baseline number produced before it is trustworthy, and the install failure must not be misdiagnosed as a submodule problem.
Output: a repaired `node_modules` tree, `packages/apps/editor/styled-system/` regenerated, and `packages/external/mota-js` initialized at the pinned SHA with `pnpm-lock.yaml` untouched.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-RESEARCH.md
@.planning/phases/01-baseline-verification-net/01-VALIDATION.md
@.planning/phases/01-baseline-verification-net/01-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Repair the workspace dependency tree and prove the editor tooling resolves</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/01-baseline-verification-net/01-RESEARCH.md (sections "Environment Availability", "Pitfall 7")
    - .planning/phases/01-baseline-verification-net/01-VALIDATION.md (section "Wave 0 Requirements")
    - .planning/phases/01-baseline-verification-net/01-CONTEXT.md (decisions D-13, D-14, D-15)
    - pnpm-workspace.yaml
    - packages/apps/editor/package.json
    - packages/apps/service-worker/package.json
  </read_first>
  <precondition>The starting `node_modules` tree is broken independently of the submodule: workspace symlinks do not resolve and invoking a store binary fails with a missing transitive module (observed this session). `E:\.pnpm-store\v10` exists.</precondition>
  <action>
    Repair the install so pnpm commands resolve again. Attempt the remediation ladder in order: `pnpm install --frozen-lockfile`; if that still aborts, remove the root `node_modules` tree and reinstall; if the content-addressable store is also damaged, `pnpm install --force`. Use the pnpm major the lockfile was written with — CI pins 11.10.0, and a local major mismatch (the research machine reported 10.15.0) is a plausible cause of the `UNKNOWN: unknown error` abort. Do NOT run `pnpm update`, do NOT edit `pnpm-lock.yaml`, do NOT add or change any dependency version, and do NOT widen the `allowBuilds` allowlist in `pnpm-workspace.yaml`. Confirm the editor's `prepare` script ran (`prepare` is `panda codegen`), so `packages/apps/editor/styled-system/` exists; if it does not, invoke `pnpm --filter @motajs/editor exec panda codegen` explicitly. Then prove the editor's installed tooling resolves from the repaired tree with `pnpm --filter @motajs/editor exec vitest --version`. Do NOT run the full unit suite here: `vite.config.ts` still hosts the `test` block and imports `./mota-root`, whose load-time guard throws while the submodule is uninitialized, so a suite run cannot start until either the submodule is materialized (Task 2) or the config is split (plan 01-06); the full-suite green check therefore belongs to Task 2. Do not misread a module-resolution error as a submodule problem — the submodule is a separate task in this plan.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest --version && git diff --exit-code -- pnpm-lock.yaml</automated>
    <fails_when>non-zero exit because the editor's vitest still cannot start from `node_modules` (the module-resolution failure this task repairs), or because the repair drifted `pnpm-lock.yaml`</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest --version` prints a 4.x version and does not raise a module-resolution error.
    - `pnpm --filter @motajs/editor test` is NOT required to pass in this task: the suite cannot even load its config while `packages/external/mota-js` is empty, because `vite.config.ts` imports `./mota-root`; its green check lives in Task 2, after the submodule is materialized.
    - `git diff --exit-code -- pnpm-lock.yaml` exits 0.
    - `packages/apps/editor/styled-system/` exists after the install.
    - `pnpm-workspace.yaml` `allowBuilds` entries are unchanged (still exactly `@parcel/watcher`, `dprint`, `esbuild`, `less`, `sharp`).
  </acceptance_criteria>
  <done>The editor's installed tooling resolves from a repaired `node_modules` (`vitest --version` succeeds where it previously failed with `Cannot find module`); the lockfile and the allowlist are untouched; `styled-system/` is regenerated. The full unit suite is re-run and required green in Task 2, once the mota-js submodule it reads is materialized.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize the mota-js submodule and verify the workspace is unchanged</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - packages/apps/editor/mota-root.ts (REQUIRED_PATHS and the load-time throw)
    - packages/apps/editor/test/utils/sampleProject.ts (the on-disk walk that unit fixtures depend on)
    - packages/apps/editor/test/blockly/uiRoundTrip.test.ts
    - packages/apps/editor/test/blockly/projectEntries.test.ts
    - packages/apps/editor/src/project/commands/__tests__/sampleProjectCommands.test.ts
    - .planning/phases/01-baseline-verification-net/01-RESEARCH.md (Assumptions Log A1, "Pitfall 1", "Pitfall 6")
    - .gitmodules
  </read_first>
  <precondition>`pnpm` resolves after Task 1, and `git submodule status packages/external/mota-js` currently prints a leading `-` (uninitialized).</precondition>
  <action>
    Run `git submodule update --init packages/external/mota-js` so the gitlink is checked out at 3efb548e407ad2b8007b498cb98401e2012a0b55. `.gitmodules` pins the SSH URL `git@github.com:ckcz123/mota-js.git`; if this clone has no GitHub SSH key, add a session-scoped rewrite with `git config url."https://github.com/".insteadOf "git@github.com:"` — do not edit `.gitmodules` and do not commit that config. Confirm Assumption A1 in the same step: `pnpm-workspace.yaml` globs `packages/external/*`, so verify the submodule root contains no `package.json` and that `pnpm install --frozen-lockfile` still succeeds with `pnpm-lock.yaml` unchanged. Then re-run the editor unit suite; the submodule-reading modules now load real mota-js files: `test/utils/sampleProject.ts` (which `src/runtime/__tests__/RuntimeResourceGateway.test.ts`, `src/project/history/__tests__/operationHistory.test.ts`, `src/project/data/__tests__/persistStatus.integration.test.ts`, `src/project/model/__tests__/projectModel.test.ts` consume), plus `test/blockly/uiRoundTrip.test.ts`, `test/blockly/projectEntries.test.ts`, and `src/project/commands/__tests__/sampleProjectCommands.test.ts`. Do not rewrite any fixture to remove the submodule dependency: the D-13 revision records that the submodule is the real prerequisite for unit tests, and the CI `unit` job checks it out.
  </action>
  <verify>
    <automated>git submodule status packages/external/mota-js && pnpm --filter @motajs/editor test</automated>
    <fails_when>the printed line begins with `-` (uninitialized) or `+` (checked-out revision differs from the recorded gitlink), or the SHA in the line is not 3efb548e407ad2b8007b498cb98401e2012a0b55, or the editor unit suite exits non-zero now that the real mota-js fixtures load</fails_when>
  </verify>
  <acceptance_criteria>
    - `git submodule status packages/external/mota-js` prints a line starting with a single space followed by 3efb548e407ad2b8007b498cb98401e2012a0b55.
    - `packages/external/mota-js/index.html`, `main.js`, `project/data.js`, and `_server/table/data.comment.js` all exist.
    - `packages/external/mota-js/package.json` does not exist.
    - `pnpm install --frozen-lockfile` exits 0 and `git diff --exit-code -- pnpm-lock.yaml` exits 0.
    - `pnpm --filter @motajs/editor test` exits 0 with a non-zero `numTotalTests` and with zero tests reported as skipped in the JSON report.
    - `.gitmodules` is unmodified.
  </acceptance_criteria>
  <done>The submodule is initialized at the pinned SHA, adds no workspace project, and the editor unit suite passes with the real mota-js fixtures loaded.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `packages/external/mota-js` (gitlink materialized) | working-tree state | VERIFY-01 |
| `packages/apps/editor/styled-system/` (regenerated) | build-generated directory (gitignored) | VERIFY-01 |
| a resolvable workspace `node_modules` tree | working-tree state (gitignored) | VERIFY-01 |

No tracked source file is modified by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local workstation → npm registry / pnpm store | `pnpm install` fetches and links third-party packages, and may execute their lifecycle scripts. |
| Local workstation → git remote (github.com) | `git submodule update` fetches third-party engine source into the checkout. |
| `packages/external/mota-js` → unit-test process | Fixtures read engine files off disk; unit tests parse them but do not execute engine code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `pnpm install` lifecycle scripts | medium | mitigate | Keep `pnpm install --frozen-lockfile`; do not widen `pnpm-workspace.yaml` `allowBuilds` (stays at the five already-approved builders); add no dependency. |
| T-01-02 | Tampering | `git submodule update` fetch | medium | mitigate | Pin to the recorded gitlink SHA; verify with `git submodule status`; never edit `.gitmodules`; the repository is public so no credentials are needed. |
| T-01-03 | Information Disclosure | local `url.<base>.insteadOf` git config | low | accept | The rewrite maps a public SSH URL to a public HTTPS URL and carries no secret; it is session-scoped and never committed. |
| T-01-SC | Tampering | package installs (supply chain) | high | mitigate | No package is added or upgraded; `--frozen-lockfile` refuses lockfile drift; RESEARCH's Package Legitimacy Audit is empty because this phase adds zero packages. No legitimacy checkpoint is required. |

</threat_model>

<verification>
1. `pnpm --filter @motajs/editor test` exits 0 from a repaired install.
2. `git submodule status packages/external/mota-js` shows the pinned SHA with no leading `-` or `+`.
3. `git diff --exit-code -- pnpm-lock.yaml` and `git diff --exit-code -- .gitmodules` both exit 0.
4. The only new tracked-content paths in the tree are Phase 1's own `.planning/**` artifacts.
</verification>

<success_criteria>
- A test command runs at all in this checkout (the Wave 0 exit criterion distinct from "the tests pass").
- `packages/external/mota-js` is initialized at 3efb548e407ad2b8007b498cb98401e2012a0b55 and adds no pnpm workspace project.
- The editor unit suite is green with the real mota-js fixtures loaded.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-01-SUMMARY.md` when done.
</output>
