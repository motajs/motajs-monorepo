---
phase: 01-baseline-verification-net
plan: 10
type: execute
wave: 7
depends_on:
  - 01-09
files_modified:
  - package.json
  - .github/workflows/deploy-editor-h5test.yml
autonomous: true
requirements:
  - VERIFY-02
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The repository declares ONE pnpm version: root `package.json` carries `packageManager: \"pnpm@12.5.1\"` and `.github/workflows/deploy-editor-h5test.yml` pins `pnpm/action-setup@v4` to `12.5.1`."
    - "`pnpm install --frozen-lockfile` under pnpm 12.5.1 exits 0 and leaves `pnpm-lock.yaml` byte-identical (header stays `lockfileVersion: '9.0'`)."
    - "The editor production build and the workspace typecheck still pass under pnpm 12.5.1."
    - "No stale `11.10.0` pnpm pin remains in tracked files."
  artifacts:
    - "package.json — adds `\"packageManager\": \"pnpm@12.5.1\"`"
    - ".github/workflows/deploy-editor-h5test.yml — `pnpm/action-setup@v4` `version:` becomes `12.5.1`"
  key_links:
    - "plan 01-07's `.github/workflows/ci.yml` mirrors this workflow's toolchain pinning exactly, so settling the pin here is what makes the new PR CI consistent with local."
    - "`pnpm-lock.yaml` was written at `lockfileVersion: '9.0'`; pnpm 12.5.1 reads it without rewriting it, which is why the frozen install stays clean."
---

<objective>
Adopt pnpm 12.5.1 as the single project toolchain version, replacing the previously pinned 11.10.0, so local and CI resolve dependencies with the same pnpm major.

Purpose: the local toolchain was upgraded to pnpm 12.5.1 by user decision. The repository still pins pnpm `11.10.0` in the only CI workflow and declares no `packageManager`, so local and CI had drifted with no mechanism to detect it. Plan 01-07 will mirror the deploy workflow's toolchain pinning into the new PR CI, so the pin must be settled here first, or the new gate would be pinned to a version nobody runs locally.
Output: a single declared pnpm version in the manifest and in the deploy workflow, with the frozen install, build and typecheck re-proven under it.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/codebase/STACK.md
@package.json
@.github/workflows/deploy-editor-h5test.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Pin pnpm 12.5.1 in the manifest and the deploy workflow</name>
  <files>package.json, .github/workflows/deploy-editor-h5test.yml</files>
  <read_first>
    - package.json (root manifest — confirm there is no existing `packageManager` field and note the root scripts)
    - .github/workflows/deploy-editor-h5test.yml (the `pnpm/action-setup@v4` step and its `version: 11.10.0` input)
    - pnpm-lock.yaml (header only — `lockfileVersion: '9.0'`)
    - .planning/codebase/STACK.md (records the previous `11.10.0` pin and the "no packageManager field" fact)
  </read_first>
  <precondition>Local pnpm reports 12.5.1, and `pnpm install --frozen-lockfile` exits 0 with no `pnpm-lock.yaml` diff before this task starts.</precondition>
  <action>
    Add `"packageManager": "pnpm@12.5.1"` to the root `package.json`. Do NOT add an `engines.pnpm` field and do not change any script or dependency. In `.github/workflows/deploy-editor-h5test.yml`, change only the `version:` input of the `pnpm/action-setup@v4` step from `11.10.0` to `12.5.1`; leave the Node version, cache setting, checkout step, jobs, and every other line untouched. Then prove the toolchain resolves with the new pin: run `pnpm install --frozen-lockfile` (with the local proxy `http://127.0.0.1:7890` if the registry needs it) and confirm it exits 0 and leaves the lockfile unchanged.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile &amp;&amp; git diff --exit-code -- pnpm-lock.yaml</automated>
    <fails_when>the install exits non-zero, or `pnpm-lock.yaml` differs after the install, or the workflow's pnpm `version:` input is not exactly `12.5.1`</fails_when>
  </verify>
  <acceptance_criteria>
    - Root `package.json` contains `"packageManager": "pnpm@12.5.1"` and no `engines` field was added.
    - `.github/workflows/deploy-editor-h5test.yml` pins `version: 12.5.1` for `pnpm/action-setup@v4`, and `git diff` for that file shows only that one changed line.
    - `pnpm install --frozen-lockfile` exits 0 and `git diff --exit-code -- pnpm-lock.yaml` exits 0.
    - The lockfile header still reads `lockfileVersion: '9.0'`.
  </acceptance_criteria>
  <done>One pnpm version is declared in the manifest and the deploy workflow, and the frozen install is unchanged under it.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the build and typecheck under pnpm 12.5.1 and confirm no stale pin remains</name>
  <files>package.json, .github/workflows/deploy-editor-h5test.yml</files>
  <read_first>
    - package.json (root `typecheck`/`build` fan-out scripts)
    - .github/workflows/deploy-editor-h5test.yml
    - packages/apps/editor/package.json (the editor `build` script and its 20 MiB artifact plugin wiring)
  </read_first>
  <precondition>Task 1 is complete and the frozen install passed under pnpm 12.5.1.</precondition>
  <action>
    Re-prove the workspace under the new toolchain: run the editor production build (`pnpm --filter @motajs/editor build`) and the workspace typecheck (`pnpm typecheck`). Then confirm no stale pnpm pin survives in tracked files: run `git grep -n "11.10.0" -- . ":!packages/external" ":!.planning"` and require no match. Record in the SUMMARY two facts this plan does not change: pnpm 12 performs an additional default step (`Verifying lockfile against supply-chain policies`) that pnpm 11 did not, and Node locally is v22 while CI uses Node 24.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor build &amp;&amp; pnpm typecheck</automated>
    <fails_when>the editor build exits non-zero, or `pnpm typecheck` exits non-zero, or the emitted artifact's raw byte total exceeds 20971520</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor build` exits 0 and the artifact raw size stays at or below the 20 MiB budget.
    - `pnpm typecheck` exits 0 across every workspace package.
    - `git grep -n "11.10.0" -- . ":!packages/external" ":!.planning"` returns no match.
    - The SUMMARY records the pnpm 12 supply-chain-verification step and the local-Node-22 vs CI-Node-24 gap as known, unchanged facts.
  </acceptance_criteria>
  <done>The workspace builds and typechecks under pnpm 12.5.1, and no tracked file still pins pnpm 11.10.0.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `package.json` `packageManager` field | toolchain declaration | VERIFY-02 |
| `.github/workflows/deploy-editor-h5test.yml` pnpm pin | CI toolchain | VERIFY-02 |

No production source file is modified by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local workstation → npm registry | `pnpm install` under the new major fetches and links packages and may run lifecycle scripts. |
| CI runner → npm registry | The deploy workflow installs with the pinned pnpm version. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | pnpm major upgrade changes resolution | high | mitigate | `--frozen-lockfile` is kept, the lockfile is asserted byte-identical, and no dependency version is added or changed by this plan. |
| T-10-02 | Tampering | pnpm 12 supply-chain policy verification | low | accept | pnpm 12's new default verification step only refuses entries that violate supply-chain policy; it is a stricter default, not a weaker one, and it passed locally on all 1209 entries. |
| T-10-SC | Tampering | package installs (supply chain) | low | mitigate | This plan adds and upgrades zero packages; it changes only the toolchain pin. |
</threat_model>

<verification>
1. `pnpm install --frozen-lockfile` exits 0 with `pnpm-lock.yaml` byte-identical.
2. `pnpm --filter @motajs/editor build` and `pnpm typecheck` both exit 0.
3. `git grep "11.10.0"` finds no tracked pin.
4. `package.json` declares `packageManager: pnpm@12.5.1`; the deploy workflow pins the same version and nothing else changed in it.
</verification>

<success_criteria>
- One pnpm version (12.5.1) is declared in both the manifest and the deploy workflow.
- The frozen install, the editor build, and the workspace typecheck all pass under it.
- No stale 11.10.0 pin remains in tracked files.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-10-SUMMARY.md` when done.
</output>
