---
phase: 01-baseline-verification-net
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - packages/apps/editor/src/project/history/__tests__/operationHistory.invariants.test.ts
autonomous: true
requirements:
  - VERIFY-04
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The history holds at most 100 entries: after 101 non-empty commits, 100 undos succeed and the next undo is a no-op."
    - "Undo and redo restore prior values using the stored inverse operation, and the stored entry is replaced by the applied inverse."
    - "Committing a new operation after an undo truncates the redo tail, so redo becomes a no-op."
    - "A commit that reports no change records no history entry."
    - "A multi-target checkpoint captures every distinct target once (de-duplicated by key), restores them in reverse order, and a failed apply leaves every target at its pre-apply value with no undo entry created."
    - "A mid-composite failure recovers already-completed children through their semantic inverses and tags the original error with the failing child's stage."
    - "After `resource.patch([...])` routed through the history, after an undo, and after a direct `resource.set(next)` — the two named entry points in VERIFY-04/D-11 — a reader of the resource observes the new value within the same microtask chain, without a re-read and without an effect flushing a timer."
  artifacts:
    - "packages/apps/editor/src/project/history/__tests__/operationHistory.invariants.test.ts"
  key_links:
    - "capacity is frozen behaviorally through execute/undo counts, because operationHistory exposes no entries getter"
    - "reactivity is asserted on the observable resource value, never on signal scheduling internals"
    - "the suite observes the module-level operationHistory singleton, then clears it in beforeEach/afterEach so tests do not leak"
---

<objective>
Freeze the operation-history invariants that Phase 4 relocates into `lib/edit/*`, covering capacity, inverse application, multi-target checkpoint rollback, composite failure recovery, and post-write resource reactivity.

Purpose: `operationHistory` is the single write path every edit flows through, and its inverse/checkpoint machinery is the mechanism that makes "behavior unchanged" checkable after the move. D-10 requires the suite to be co-located in `__tests__/`. D-11 scopes this plan to the named invariants and explicitly forbids recording full state-transition snapshots, so the suite must not over-fit internal structure. This plan depends on plan 01-02 so the recorded baseline describes the pre-Phase-1 unit suite, keeping the characterization suites a deliberate, visible addition.
Output: `src/project/history/__tests__/operationHistory.invariants.test.ts`, co-located beside the untouched existing `operationHistory.test.ts`.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-RESEARCH.md
@.planning/phases/01-baseline-verification-net/01-PATTERNS.md
@.planning/codebase/TESTING.md
@packages/apps/editor/src/project/history/operationHistory.ts
@packages/apps/editor/src/project/history/operations.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze history capacity, inverse application and redo truncation</name>
  <files>packages/apps/editor/src/project/history/__tests__/operationHistory.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/project/history/operationHistory.ts
    - packages/apps/editor/src/project/history/operations.ts
    - packages/apps/editor/src/project/history/viewport.ts
    - packages/apps/editor/src/project/history/__tests__/operationHistory.test.ts (the untouched analog, including its local counterOperation helper)
    - .planning/phases/01-baseline-verification-net/01-PATTERNS.md (section "operationHistory.invariants.test.ts")
  </read_first>
  <precondition>Plan 01-01 is complete; `pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.test.ts` passes.</precondition>
  <action>
    Create `src/project/history/__tests__/operationHistory.invariants.test.ts`. Use a pure in-memory `EditorOperation` factory with an empty `targets` array and a closure counter, mirroring the existing spec's local counterOperation helper, so these tests need no project fixture and no mota-js submodule. Clear the `operationHistory` singleton in `beforeEach` and `afterEach`. Freeze four behaviours. First, capacity without reading internals: `operationHistory` exposes no entries getter, so observe it behaviorally — execute 101 counter operations that each report `changed: true`, then assert 100 consecutive `undo()` calls each restore a distinct prior value and that the 101st `undo()` leaves the value unchanged (the oldest entry was evicted). Second, inverse application: after execute-then-undo the observable value returns to its prior state, and a following `redo()` re-applies the value, proving the stored operation was replaced by the applied inverse rather than re-run verbatim. Third, redo truncation: execute, undo, execute a different value, then `redo()` is a no-op. Fourth, a commit whose `apply` returns `changed: false` records nothing, evidenced by an `undo()` immediately afterward being a no-op. Do not register an editor viewport provider — `captureEditorViewport()` returns null without one and `restoreEditorViewport(null)` is a no-op, which keeps these tests free of viewport concerns. Derive every expectation observation-first rather than from reading the implementation, and do not assert `operationHistory` internals or record a transition snapshot.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or vitest reports zero tests collected, or any test in the file is skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.invariants.test.ts` exits 0 with every test passed and none skipped.
    - The capacity test drives 101 changed commits and asserts 100 successful undos plus a no-op 101st undo, without accessing any private entries array.
    - The file contains a redo-truncation test and a no-change-records-nothing test.
    - The file imports the `operationHistory` singleton and clears it in both `beforeEach` and `afterEach`.
    - The file does not register an editor viewport provider and contains no `loadSampleProject` import, so it runs without the mota-js submodule.
    - `git diff --exit-code -- packages/apps/editor/src/project/history/__tests__/operationHistory.test.ts` exits 0.
  </acceptance_criteria>
  <done>Capacity 100, inverse application, redo truncation and the no-change no-op are frozen behaviorally, in a suite that needs no project fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze multi-target checkpoint rollback and composite failure recovery</name>
  <files>packages/apps/editor/src/project/history/__tests__/operationHistory.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/project/history/operationHistory.ts (captureTargets, restoreTargets, applyWithCheckpoint)
    - packages/apps/editor/src/project/history/operations.ts (CompositeOperation apply and its commandStage tagging)
    - packages/apps/editor/src/project/history/__tests__/operationHistory.test.ts (the custom OperationTarget and composite-recovery analogs)
  </read_first>
  <precondition>Task 1 is complete and the file runs green so a new failure can be attributed to this task's additions.</precondition>
  <action>
    Extend the same file with the checkpoint and composite invariants. Build custom `OperationTarget` objects with `key`, `path`, `capture` and `restore`, and drive them through operations whose `apply` mutates then either succeeds or throws. Freeze: (1) de-duplication by `key` — pass the same target twice plus one distinct target and assert each distinct target is captured and restored exactly once (observe via counters on the target objects, not via internals); (2) reverse-order restore — assert the restore call order is the reverse of the capture order for the distinct targets; (3) a failed apply after a mutation leaves every target back at its pre-apply value and leaves the history with no new entry, so a following `undo()` is a no-op; (4) a composite whose child fails recovers every already-completed changed child through its semantic inverse, restores the counter to its starting value, and rejects with an error carrying `commandStage` equal to the failing child's stage. Reuse the existing spec's approach of a closure counter plus a failing child operation. Observation-first for all expectations.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or vitest reports fewer tests collected than after Task 1 (a regression that removed coverage), or any test in the file is skipped</fails_when>
  </verify>
  <acceptance_criteria>
    - The suite still exits 0 and every test passes.
    - The file asserts target de-duplication by `key` through per-target capture and restore counters.
    - The file asserts restore order is the reverse of capture order.
    - The file asserts that after a failed apply every target holds its pre-apply value and a subsequent `undo()` is a no-op.
    - The file asserts a composite child failure rejects with `commandStage` equal to the failing child's stage and that the shared counter returns to its starting value.
    - The file still contains no `loadSampleProject` import.
  </acceptance_criteria>
  <done>Multi-target checkpoint capture, de-duplication, reverse-order restore and composite failure recovery are frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze resource reactivity after patch, after set and after undo</name>
  <files>packages/apps/editor/src/project/history/__tests__/operationHistory.invariants.test.ts</files>
  <read_first>
    - packages/apps/editor/src/project/history/__tests__/operationHistory.test.ts (the memory-first reactivity analog)
    - packages/apps/editor/src/project/data/__tests__/persistStatus.integration.test.ts (fixture and singleton reset idiom)
    - packages/apps/editor/test/utils/sampleProject.ts
    - packages/apps/editor/src/project/data/DataResource.ts (the `set(next)` method at line 101 — the second named entry point beside `patch`)
    - packages/apps/editor/src/project/commands/tableCommands.ts
    - packages/apps/editor/src/project/data/projectData.ts
  </read_first>
  <precondition>Plan 01-01 is complete so `MOTA_JS_ROOT/project` is readable, and the editor vitest configuration aliases `@test` to `test`.</precondition>
  <action>
    Extend the same file with the reactivity invariant, isolating it in its own `describe` because it is the only submodule-coupled part. Load the real fixture with `loadSampleProject()` in that describe's `beforeEach`, and reset `operationHistory`, `FileHandlerManager`, and `projectData` in `afterEach` exactly as the existing spec does. Set a write delay on the fixture's `MemoryFileSystem` so the disk write is still outstanding, then route a floor title change through `tableCommands.patchFloor` (which flows through the history) and assert that reading the floor resource's value yields the new title immediately, within the same microtask chain — no re-read, no timer wait, no effect flush. Then await `operationHistory.undo()` and assert the resource value returns to the original title while the write is still pending, and that flushing persistence leaves the on-disk content without the discarded title. Add a second test in the same describe for the other named entry point, `resource.set(next)`: set a write delay on the fixture's `MemoryFileSystem` again (each test reloads the fixture in `beforeEach`), call `await floorResource.set({ ...floorResource.value(), title: "Set direct title" })` on the floor resource, and assert that `floorResource.value().title` equals the new title before any persistence flush, proving a direct `set` — which does not flow through the history — is memory-first in exactly the same way as the history-routed `patch`; then flush persistence and assert the on-disk content contains the new title. Assert only the observable resource value; never assert signal scheduling internals. Keep this describe free of viewport registration and keep the rest of the file free of the fixture.
  </action>
  <verify>
    <automated>pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.invariants.test.ts</automated>
    <fails_when>exit code is non-zero, or the reactivity test does not observe the patched value before awaiting persistence, or the direct-`set` test does not observe the set value before awaiting persistence, or the run fails at collection with a mota-js root error (plan 01-01's submodule not in place)</fails_when>
  </verify>
  <acceptance_criteria>
    - The suite still exits 0 with every test passed.
    - The reactivity describe asserts the new floor title is observable on the resource value immediately after the patch call, before any persistence await.
    - The patch test asserts the title reverts after `operationHistory.undo()` and that the flushed on-disk content does not contain the discarded title.
    - The reactivity describe contains a second test that asserts a direct `floorResource.set(...)` new value is observable before any persistence await, so both VERIFY-04/D-11 entry points (`set` and `patch`) are frozen.
    - The fixture-coupled assertions live in one `describe`; the rest of the file remains free of `loadSampleProject`.
    - No assertion references signal effect timing or private scheduling state.
  </acceptance_criteria>
  <done>Reactivity after a history-routed patch, after a direct `set`, and after undo is frozen on the observable resource value, with the submodule-coupled assertions isolated to one describe block.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| `src/project/history/__tests__/operationHistory.invariants.test.ts` | characterization suite | VERIFY-04 |

No production source file is modified: the existing `operationHistory.test.ts` stays untouched per D-09.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → history semantics | An over-fitted assertion becomes the definition of correct behavior for the moved `lib/edit/*` layer. |
| Test fixture → on-disk sample project | The reactivity test reads real mota-js floor files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | capacity assertion | medium | mitigate | Freeze capacity behaviorally through execute/undo counts rather than by reading the private entries array, so the assertion tests the contract and survives the Phase 4 relocation. |
| T-04-02 | Tampering | reactivity assertion | medium | mitigate | Assert the observable resource value only; asserting scheduler internals would over-fit Vitest's microtask model and break spuriously after the move. |
| T-04-03 | Repudiation | a suite that cannot fail | high | mitigate | Confirm the suite is red against one deliberate implementation break (for example disabling the capacity eviction or the inverse swap), then revert the break before committing. |
| T-04-SC | Tampering | test-only installs | low | accept | No package is added; vitest and the existing fixtures are reused. |

</threat_model>

<verification>
1. `pnpm --filter @motajs/editor exec vitest run src/project/history/__tests__/operationHistory.invariants.test.ts` exits 0.
2. `git diff --exit-code -- packages/apps/editor/src/project/history/__tests__/operationHistory.test.ts` exits 0.
3. The capacity, inverse, redo-truncation and checkpoint tests run without the mota-js submodule; only the reactivity describe needs it.
4. The suite has been observed red against one deliberate break, then green after the revert.
</verification>

<success_criteria>
- All seven history invariants named by VERIFY-04 are frozen by executable tests.
- Both reactivity entry points named by D-11 — `patch` and `set` — are asserted on the observable resource value within the same microtask chain.
- A regression in capacity, inverse application, checkpoint capture/restore, or reactivity turns an assertion red.
- The existing history suite is unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-04-SUMMARY.md` when done.
</output>
