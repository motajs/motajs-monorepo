---
phase: 01-baseline-verification-net
plan: 09
type: execute
wave: 6
depends_on:
  - 01-08
files_modified:
  - scripts/verify/lint-severities.js
  - packages/apps/editor/src/Workbench/AppendPicPanel/index.tsx
  - packages/apps/editor/src/utils/canvas/detectWhiteBackground.ts
  - packages/apps/editor/src/blockly/registry/path.ts
  - packages/apps/service-worker/src/server/fsApi.ts
  - packages/apps/editor/src/runtime/iframeEntry.ts
  - packages/apps/editor/src/runtime/RuntimeProvider.tsx
  - packages/apps/editor/src/project/model/tableModels.ts
  - packages/apps/editor/src/project/model/__tests__/tableModels.test.ts
  - packages/apps/editor/src/project/commands/__tests__/sampleProjectCommands.test.ts
  - packages/apps/editor/src/services/tableMeta/__tests__/tableMetaService.test.ts
  - packages/apps/editor/src/services/tower/__tests__/towerService.test.ts
  - packages/apps/editor/src/hooks/useFs.ts
  - packages/apps/editor/src/hooks/useImageAssetUrl.ts
  - packages/apps/editor/src/MapEditor/rendering/MapPixiRenderer.tsx
  - packages/apps/editor/src/Workbench/modals/SearchFlags/useSearchFlagsModal.tsx
  - packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx
  - packages/apps/editor/src/Workbench/modals/SelectPoint/useSelectPointModal.tsx
  - packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointShell.tsx
  - packages/apps/editor/src/Workbench/modals/StatusBarPreview/StatusBarPreviewContent.tsx
  - packages/apps/editor/src/Workbench/EventsEditor/BlocklyCapabilitiesContext.tsx
  - packages/apps/editor/src/Workbench/EventsEditor/EventEditorContext.tsx
  - packages/apps/editor/src/Workbench/modals/CheckboxSet/CheckboxSetModalContext.tsx
  - packages/apps/editor/src/components/ContentBoundary/index.tsx
  - packages/apps/editor/src/Workbench/components/PanelSlot.tsx
  - packages/apps/editor/src/components/Table/index.tsx
  - packages/apps/editor/e2e/core-panel-write.spec.ts
  - packages/apps/editor/eslint.config.js
autonomous: true
requirements:
  - VERIFY-02
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "`pnpm lint` — exactly `eslint .` with no `--fix` and no warning cap — exits 0, so the lint layer of the four-job PR gate can finally be green."
    - "`pnpm --filter @motajs/editor lint` also exits 0, so the editor's own config standing alone is green and the two entry points agree."
    - "Every non-stylistic error from the 46-error baseline is resolved; what remains is warnings only."
    - "No rule was weakened: no severity was downgraded, no rule was disabled, no source path was newly ignored, and no `--max-warnings` escape was introduced anywhere."
    - "Every narrow exception is a single-line disable carrying a concrete reason, and none of them changed runtime behaviour — `pnpm typecheck`, the editor unit suite and `pnpm build` stay green."
  artifacts:
    - "scripts/verify/lint-severities.js — asserts the previously-erroring rules are still errors and that every disable comment carries a reason"
    - "zero ESLint errors under the root config and under the editor config"
    - "narrow, reason-carrying per-line disables where a behaviour-preserving fix was not safe"
  key_links:
    - "`eslint . --format json` error count is the gate's own measure: 0 errors, warnings permitted; the per-rule counts are the task boundaries of this plan"
    - "plan 01-07's CI `lint` job calls the root `lint` script, so this plan's green gate is what finally unblocks the four-job workflow it publishes"
    - "`@typescript-eslint/no-explicit-any` is an error in the editor config but a warning in the shared root block, so that rule's 21 occurrences are editor-config errors and are the largest single block of this plan"
    - "the lint report is written to a gitignored `node_modules/` path so the gate never adds an untracked artifact to the repo"
---

<objective>
Make `pnpm lint` exit 0 by fixing the 42 remaining pre-existing errors the baseline recorded, without weakening a single rule and without changing runtime behaviour — the pure-refactor constraint that governs this whole milestone.

Purpose: plan 01-06 delivered the non-fixing `eslint .` gate, but the four-layer PR gate of D-01 cannot block merges on a permanently red lint layer, and D-04 forbids an advisory period — so the debt has to be fixed rather than tolerated. D-02 requires the gate to cover every workspace package, and plan 01-08 removed the 4 formatting and generated-output errors, leaving 42 real rule violations that no gate had ever covered because the old root `lint` script was `eslint --fix` and a root `eslint .` used to run out of memory on the vendored submodule. Plan 01-07 wires the lint layer of the four-job PR gate to this script, so publishing that gate requires this plan to land first; the milestone's own rule — no behaviour change, ever — means most of these must be resolved with precise types, narrow per-line exceptions carrying reasons, or config options that narrow a false-positive class rather than silence a rule.
Output: zero ESLint errors under both the root and the editor config, a repo-owned severity/disable-comment verifier, and a task-by-task record of which sites got a real fix and which got a justified exception.
</objective>

<execution_context>
@C:/Users/book/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/book/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-verification-net/01-CONTEXT.md
@.planning/phases/01-baseline-verification-net/01-08-SUMMARY.md
@.planning/phases/01-baseline-verification-net/01-06-SUMMARY.md
@.planning/phases/01-baseline-verification-net/deferred-items.md
@.planning/codebase/CONVENTIONS.md
@eslint.config.js
@packages/apps/editor/eslint.config.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Clear the four mechanical errors and prove the gate actually moves</name>
  <files>packages/apps/editor/src/Workbench/AppendPicPanel/index.tsx, packages/apps/editor/src/utils/canvas/detectWhiteBackground.ts, packages/apps/editor/src/blockly/registry/path.ts, packages/apps/service-worker/src/server/fsApi.ts, scripts/verify/lint-severities.js</files>
  <read_first>
    - packages/apps/editor/src/Workbench/AppendPicPanel/index.tsx (around line 197 — the `source` binding `prefer-const` flags)
    - packages/apps/editor/src/utils/canvas/detectWhiteBackground.ts (the whole file; note that the only assignment to `black` is a commented-out line, so the variable is permanently zero)
    - packages/apps/editor/src/blockly/registry/path.ts (line 8 — the escaped opening bracket inside the character class)
    - packages/apps/service-worker/src/server/fsApi.ts (line 13 — the control-character regex inside `normalizeProjectPath`, a path-safety guard that must keep rejecting exactly the same inputs)
    - packages/apps/editor/src/blockly/registry/__tests__/ if it exists, and any test covering `normalizeProjectPath` / `parseDataPath` (these four edits must not change observable results)
    - scripts/baseline/collect.js (the repo-owned Node script style the new verifier should match)
  </read_first>
  <precondition>Plan 01-08 is complete: the tree is Prettier-clean, `format:check` is green, and `pnpm lint` currently reports the 42 non-formatting baseline errors. The line and column numbers in the baseline inventory are PRE-formatting and have shifted; relocate every site by rule id and message, not by coordinates.</precondition>
  <action>
    This is the tracer for the plan: four mechanical, semantics-preserving fixes that prove the whole loop — locate the error from the lint report, fix it, re-run the gate, watch the count drop. Do all four, then start the verifier.

    First, `packages/apps/editor/src/Workbench/AppendPicPanel/index.tsx`: the flagged binding is never reassigned, so change its declaration keyword to the constant form. Nothing else in the statement changes.

    Second, `packages/apps/editor/src/utils/canvas/detectWhiteBackground.ts`: the flagged binding is `black`, which is only ever written by a commented-out line and is therefore permanently zero, while `trans` and `white` are genuinely incremented in the loop and must keep the mutable declaration. Split `black` out into its own constant-zero declaration carrying a short comment that the black-pixel branch is disabled. Keep the return expression's shape — the comparison against `black` must stay so the resulting truth value is provably identical (it reduces to the same expression as before).

    Third, `packages/apps/editor/src/blockly/registry/path.ts`: remove the unnecessary backslash escape on the opening bracket inside the character class. The closing bracket's escape must stay, because an unescaped closing bracket terminates the class and would change the pattern into something that matches differently. The accepted and rejected data paths must be exactly the same set as before — if a test covers `parseDataPath`, run it.

    Fourth, `packages/apps/service-worker/src/server/fsApi.ts`: the control-character regex is a deliberate path-safety guard, so it must keep rejecting exactly U+0000 through U+001F and U+007F through U+009F, and must keep short-circuiting with the backslash and leading-slash checks in the same order. Replace the regex with a small module-local helper that scans the string's character codes and reports whether any falls in those two ranges, and call it in the guard's place. Do not move, reorder, or relax any other check in `normalizeProjectPath`, and do not touch the error type or message. A per-line disable would also satisfy the rule, but a helper is the real fix here and is cheap, so take it; if you find that a code-unit scan cannot be made exactly equivalent, stop and use a narrow disable with the reason instead, and say so.

    Then create `scripts/verify/lint-severities.js` — a dependency-free Node script matching `scripts/baseline/collect.js`'s style — which will be this plan's "no rule was weakened" proof for the remaining tasks. It must: spawn `eslint --print-config` for one editor file and one non-editor file, parse the JSON it prints, and fail unless each rule in a named list still resolves to the severity it had before this plan — `@typescript-eslint/no-explicit-any` (error in the editor config, warning in the shared root block), `react-refresh/only-export-components`, `react-hooks/set-state-in-effect`, `prefer-const`, `no-control-regex`, `no-useless-escape`, `react-hooks/use-memo` and `react-hooks/immutability` all still active in the editor config. It must also walk the tracked source tree with `git ls-files`, find every `eslint-disable` / `eslint-disable-next-line` / `eslint-disable-line` comment, and fail if any of them lacks a reason after a ` -- ` separator with non-empty text; print the total count of disable comments so the SUMMARY can cite it. Exit non-zero with a one-line reason per failed assertion.

    Do NOT reach for `eslint --fix` at any point in this plan: the gate is `eslint .` with no fixer, and a fixer run would silently rewrite files outside the sites under discussion. Run `pnpm lint` to observe the count, then re-run it after all four edits.
  </action>
  <verify>
    <automated>node scripts/verify/lint-severities.js; pnpm exec eslint . --format json -o node_modules/.gsd-lint.json; node -e "const r=require('./node_modules/.gsd-lint.json');const bad=[];for(const f of r)for(const m of f.messages)if(m.severity===2&&['no-useless-escape','no-control-regex','prefer-const'].includes(m.ruleId))bad.push(f.filePath+':'+m.line+' '+m.ruleId);if(bad.length){console.error(bad.join('\n'));process.exit(1)}let errs=0;for(const f of r)for(const m of f.messages)if(m.severity===2)errs++;console.log('remaining errors: '+errs)"</automated>
    <fails_when>the severity verifier exits non-zero, or the lint report still contains an error under any of `no-useless-escape`, `no-control-regex` or `prefer-const`, or the printed remaining-error count is not exactly 42 minus 4</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/verify/lint-severities.js` exists and exits 0, asserting the named rules unchanged and every existing disable comment reason-carrying.
    - The lint report contains zero errors under `no-useless-escape`, `no-control-regex` and `prefer-const`, and the total error count is exactly 4 lower than the 42 this plan started from.
    - `packages/apps/editor/src/utils/canvas/detectWhiteBackground.ts` still returns the identical boolean for the same `ImageData` (the comparison against the never-assigned zero value is preserved).
    - Any test covering `parseDataPath` or `normalizeProjectPath` exits 0, and the accepted/rejected input sets are unchanged.
    - No `eslint --fix` was run: on the uncommitted working tree, `git diff --name-only` lists only the four named sites plus the new verifier.
    - No rule severity changed and no ignore list grew.
  </acceptance_criteria>
  <done>The four mechanical errors are fixed with provably identical behaviour, the error count has dropped by exactly four, and the plan's standing "no rule weakened" verifier exists.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 21 `@typescript-eslint/no-explicit-any` errors without relaxing the rule</name>
  <files>packages/apps/editor/src/runtime/iframeEntry.ts, packages/apps/editor/src/project/model/tableModels.ts, packages/apps/editor/src/project/model/__tests__/tableModels.test.ts, packages/apps/editor/src/project/commands/__tests__/sampleProjectCommands.test.ts, packages/apps/editor/src/services/tableMeta/__tests__/tableMetaService.test.ts, packages/apps/editor/src/services/tower/__tests__/towerService.test.ts, packages/apps/editor/e2e/core-panel-write.spec.ts</files>
  <read_first>
    - packages/apps/editor/src/runtime/iframeEntry.ts (the `runtime` bridge declaration and each `const core` assignment inside the preview entry points)
    - packages/apps/editor/src/project/model/tableModels.ts (the local AST node alias, and the two arithmetic operand casts inside the binary-expression branch of `safeValue`)
    - packages/apps/editor/src/services/tower/__tests__/towerService.test.ts (the private-field reset and injection in `beforeEach`/`afterEach`/`setupTowerData`)
    - packages/apps/editor/src/fs/FileHandlerManager.ts (confirm `handlers` is private and only `clear()` is public — this determines whether a real fix exists for the two test files that reach into it)
    - packages/apps/editor/src/project/commands/__tests__/sampleProjectCommands.test.ts and packages/apps/editor/src/project/model/__tests__/tableModels.test.ts (the deeply nested dynamic schema trees being cast)
    - packages/apps/editor/e2e/core-panel-write.spec.ts (the six `any` helper positions)
  </read_first>
  <precondition>Task 1 is complete, so the lint report's remaining errors are down to this task's rule and the React-hooks/react-refresh groups.</precondition>
  <action>
    Relocate all 21 sites from the lint report rather than from the baseline coordinates. Decide each site with this rule, in order: replace the `any` with a precise type when the type is cheap and local; otherwise use a single-line disable that names the rule and carries a concrete reason. Never downgrade the rule, never disable it file-wide or globally, and never reach for `unknown` at a site where it would only push a cascade of new casts into the surrounding code.

    Real fixes to make. In `packages/apps/editor/src/project/model/tableModels.ts`, the two operand casts inside the binary-expression branch exist only so TypeScript will accept the arithmetic operators on values `safeValue` returns loosely typed. Replace each `any` with the narrow numeric-ish cast that keeps the emitted JavaScript byte-identical: the addition branch needs a cast that still permits JavaScript's own string-or-number semantics, and the subtraction, multiplication and division branches need a numeric cast. Because casts are erased, the runtime expression is unchanged; state this in the SUMMARY. Keep the local AST node alias itself as a reason-carrying narrow disable — it is a deliberately loosely-typed walker over acorn nodes whose properties are read by dynamic name, and tightening it to a precise type would require narrowing at every property access in the file, which is a separate concern; give it a reason that says exactly that.

    Narrow reason-carrying disables, each with a site-specific reason. In `packages/apps/editor/src/runtime/iframeEntry.ts`, the runtime bridge declaration and each `core` alias are the untyped mota-js global injected by the engine that this iframe loads — the type genuinely does not exist on this side of the boundary, and the file already narrows individual values as it reads them. In `packages/apps/editor/e2e/core-panel-write.spec.ts`, the six positions are Playwright helpers over a dynamically-shaped editor window; type them only if a single precise type covers all six without altering the specs' runtime behaviour, otherwise disable each with a reason naming the browser-side bridge. In `packages/apps/editor/src/project/commands/__tests__/sampleProjectCommands.test.ts` and `packages/apps/editor/src/project/model/__tests__/tableModels.test.ts`, the casts reach into a deeply nested, dynamically-generated meta tree whose shape is the thing under test; a real type would have to be written for the fixture rather than for production code. In `packages/apps/editor/src/services/tower/__tests__/towerService.test.ts` and `packages/apps/editor/src/services/tableMeta/__tests__/tableMetaService.test.ts`, the casts reset or inject module-private handler state. Read `FileHandlerManager` first: if an existing public seam already does what the cast does, use it — that is the real fix and you should take it; if the only change that would remove the cast is adding a new production method purely for tests, do not add it, and disable with a reason that says no test seam exists. Apply the same reasoning to the tower service's private handler field.

    Record in the SUMMARY, per file, which sites got a real fix, which got a disable, the exact reason text used, and the total disable count the verifier printed.
  </action>
  <verify>
    <automated>node scripts/verify/lint-severities.js; pnpm exec eslint . --format json -o node_modules/.gsd-lint.json; node -e "const r=require('./node_modules/.gsd-lint.json');const bad=[];for(const f of r)for(const m of f.messages)if(m.severity===2&&m.ruleId==='@typescript-eslint/no-explicit-any')bad.push(f.filePath+':'+m.line);if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('no-explicit-any errors cleared')"</automated>
    <fails_when>the severity verifier exits non-zero, or the lint report still shows an error under `@typescript-eslint/no-explicit-any`, or the resolved editor config no longer reports that rule at error severity</fails_when>
  </verify>
  <acceptance_criteria>
    - The lint report contains zero errors under `@typescript-eslint/no-explicit-any`.
    - The resolved editor config still reports that rule at error severity, and the shared root block still reports it at warning severity.
    - The two operand casts in `tableModels.ts` are no longer `any` and the emitted expression is unchanged (casts are erased).
    - Each remaining disable is a single-line comment naming the rule and carrying a reason.
    - `pnpm typecheck` exits 0 and the editor unit suite exits 0, proving no test or production behaviour depended on the removed `any`.
  </acceptance_criteria>
  <done>Every `no-explicit-any` error is either replaced by a precise type or covered by a narrow, reasoned exception; the rule itself is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the 7 `react-hooks/set-state-in-effect` errors</name>
  <files>packages/apps/editor/src/MapEditor/rendering/MapPixiRenderer.tsx, packages/apps/editor/src/hooks/useImageAssetUrl.ts, packages/apps/editor/src/Workbench/modals/SearchFlags/useSearchFlagsModal.tsx, packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx, packages/apps/editor/src/Workbench/modals/StatusBarPreview/StatusBarPreviewContent.tsx</files>
  <read_first>
    - packages/apps/editor/src/MapEditor/rendering/MapPixiRenderer.tsx (the empty-paths branch that destroys the ref-held textures and then resets state)
    - packages/apps/editor/src/hooks/useImageAssetUrl.ts (the whole file — the object-URL create/revoke lifecycle)
    - packages/apps/editor/src/Workbench/modals/SearchFlags/useSearchFlagsModal.tsx (both the `open` callback, which already seeds the selection, and the effect that seeds it again when the index arrives late)
    - packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx (the clamping effect and the state it clamps)
    - packages/apps/editor/src/Workbench/modals/StatusBarPreview/StatusBarPreviewContent.tsx (the three effects: flag derivation, lease drive, lease close on runtime error)
    - packages/apps/editor/src/runtime/RuntimeProvider.tsx (the surrounding provider, to confirm none of these effects participate in the runtime handshake)
  </read_first>
  <precondition>Task 2 is complete. This rule comes from the React Compiler aware react-hooks plugin and is behaviour-sensitive: the milestone forbids behaviour change, so every decision here must be justified against that constraint rather than against cleanliness.</precondition>
  <action>
    Decide each of the seven sites on one test: is there a change that removes the synchronous `setState` from the effect body while leaving every rendered prop, every effect dependency array and every commit ordering exactly as they are? Only if the answer is provably yes, make that change. Otherwise leave the code alone and add a single-line disable naming `react-hooks/set-state-in-effect` with a site-specific reason. Do not change runtime behaviour to satisfy a linter on a milestone whose whole premise is that behaviour does not change.

    Work through them, and record the answer per site in the SUMMARY. `MapPixiRenderer.tsx`: the effect destroys ref-held Pixi textures and then resets React state in the same pass, so the state reset is the visible consequence of an external-system teardown rather than derived state. `useImageAssetUrl.ts`: the effect creates an object URL and stores it together with its revision, and its cleanup revokes that same URL — the value cannot be derived because the revocation lifecycle is the point. `useSearchFlagsModal.tsx`: the effect seeds a controlled select's value once a lazily loaded flag index arrives; deriving the value instead would change the select's controlled value prop on first render, which is observable. `SelectPointContent.tsx`: the effect clamps stored position state when the map's dimensions shrink, and the clamp has to persist back into state rather than be recomputed on render. `StatusBarPreviewContent.tsx` has three: deriving the flags field from the script text inline would re-parse user code on every render where the effect parses only when the code changes; the lease-driving effect synchronously invokes an async callback that sets state; and the third closes a lease when the runtime reports an error, which is external-system cleanup. All seven are expected to end as narrow disables — if you conclude that for any one of them a genuine zero-difference refactor exists, make it, and say in the SUMMARY exactly why it was zero-difference.

    The reason text must be specific to the site. Do not reuse one generic string across all seven, and do not add a rule-level option or a config-level exception for this rule.
  </action>
  <verify>
    <automated>node scripts/verify/lint-severities.js; pnpm exec eslint . --format json -o node_modules/.gsd-lint.json; node -e "const r=require('./node_modules/.gsd-lint.json');const bad=[];for(const f of r)for(const m of f.messages)if(m.severity===2&&m.ruleId==='react-hooks/set-state-in-effect')bad.push(f.filePath+':'+m.line);if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('set-state-in-effect errors cleared')"</automated>
    <fails_when>the severity verifier exits non-zero, or the lint report still shows an error under `react-hooks/set-state-in-effect`, or the resolved editor config no longer reports that rule at error severity</fails_when>
  </verify>
  <acceptance_criteria>
    - The lint report contains zero errors under `react-hooks/set-state-in-effect`.
    - The resolved editor config still reports that rule at error severity.
    - Each site's reason text is distinct and names that site's external synchronization.
    - No effect body, dependency array, or rendered prop was changed purely to satisfy the rule; the SUMMARY states per site whether a refactor was made or rejected and why.
    - The editor unit suite exits 0, and the runtime preview tests in particular are green.
  </acceptance_criteria>
  <done>All seven sites are resolved with the rule still enabled, and each resolution is recorded with its justification.</done>
</task>

<task type="auto">
  <name>Task 4: Resolve the 8 `react-refresh/only-export-components` errors</name>
  <files>packages/apps/editor/src/components/ContentBoundary/index.tsx, packages/apps/editor/src/Workbench/components/PanelSlot.tsx, packages/apps/editor/src/components/Table/index.tsx, packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx, packages/apps/editor/src/Workbench/modals/SelectPoint/useSelectPointModal.tsx, packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointShell.tsx, packages/apps/editor/src/Workbench/EventsEditor/BlocklyCapabilitiesContext.tsx, packages/apps/editor/src/Workbench/EventsEditor/EventEditorContext.tsx, packages/apps/editor/src/Workbench/modals/CheckboxSet/CheckboxSetModalContext.tsx, packages/apps/editor/eslint.config.js</files>
  <read_first>
    - packages/apps/editor/src/components/ContentBoundary/index.tsx (the wildcard re-export at the end of the file, whose target is the `RecoveryUI` directory barrel)
    - packages/apps/editor/src/Workbench/components/PanelSlot.tsx (the only consumer that imports `NotFoundRecovery` through the barrel; `ContentLeftTab.tsx` imports only the component and the props type and needs no change)
    - packages/apps/editor/src/components/Table/index.tsx (`openExternalEditor` re-export; `TableRow.tsx` and the externalEditor test both import the module directly, so the barrel re-export has no consumer)
    - packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx (the exported pure helper that is used only inside the same file)
    - packages/apps/editor/src/Workbench/modals/SelectPoint/useSelectPointModal.tsx (the local shell component that is defined but not exported while the file exports a hook)
    - packages/apps/editor/src/Workbench/EventsEditor/BlocklyCapabilitiesContext.tsx, packages/apps/editor/src/Workbench/EventsEditor/EventEditorContext.tsx, packages/apps/editor/src/Workbench/modals/CheckboxSet/CheckboxSetModalContext.tsx (the context-module shape: a Provider component plus its consumer hooks in one file)
    - packages/apps/editor/eslint.config.js (the `react-refresh` preset entry this task will add rule options beside)
  </read_first>
  <precondition>Task 3 is complete. The editor's react-refresh rule arrives at error severity from its own preset, so any option added must be applied after that preset to take effect.</precondition>
  <action>
    Split the eight sites by cost. Four are real fixes that are cheap and local, so take them. Four are the idiomatic React context-module shape, where the plugin's own documented option for a named non-component export is the narrower and more honest resolution than moving the context into a third module and rewiring every consumer; take that for the four, and say so plainly in the SUMMARY rather than pretending it is a fix.

    Real fixes. In `packages/apps/editor/src/components/ContentBoundary/index.tsx`, delete the wildcard re-export line and update the one consumer — `packages/apps/editor/src/Workbench/components/PanelSlot.tsx` — to import `NotFoundRecovery` straight from the `RecoveryUI` entry it actually lives in. Confirm with a project-wide search that no other file imports a `RecoveryUI` symbol through the `components/ContentBoundary` barrel before deleting. In `packages/apps/editor/src/components/Table/index.tsx`, delete the `openExternalEditor` re-export outright — its internal consumers import the module directly and the barrel re-export has none; if the search turns up an external consumer, redirect that consumer instead and note it. In `packages/apps/editor/src/Workbench/modals/SelectPoint/SelectPointContent.tsx`, drop the `export` keyword from the coordinate-parsing helper, which is only called inside that file. In `packages/apps/editor/src/Workbench/modals/SelectPoint/useSelectPointModal.tsx`, move the local shell component into a new sibling module `SelectPointShell.tsx` and import it back, so the original file exports only the hook and no local component remains; check for other consumers of the shell first and update any you find.

    Config-level narrowing. In `packages/apps/editor/eslint.config.js`, add `allowExportNames` to the existing `react-refresh/only-export-components` rule entry, listing exactly the four hook names that the three context modules export, and place the entry after the preset so it wins. This is a rule option that narrows a known false-positive class to named identifiers — it is not a severity change, not a disable, and not a wildcard, and the severity must stay exactly where 01-08 left it. Do not use `allowExportNames` for anything other than those four names, and do not add a `files`-scoped block that silences the rule for whole directories.

    If for any of the four "real fix" sites the change turns out to require touching consumers outside the listed files in a way that grows the diff beyond the local edit, stop and report it in the SUMMARY rather than expanding the change set.
  </action>
  <verify>
    <automated>node scripts/verify/lint-severities.js; pnpm exec eslint . --format json -o node_modules/.gsd-lint.json; node -e "const r=require('./node_modules/.gsd-lint.json');const bad=[];for(const f of r)for(const m of f.messages)if(m.severity===2&&m.ruleId==='react-refresh/only-export-components')bad.push(f.filePath+':'+m.line);if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log('only-export-components errors cleared')"</automated>
    <fails_when>the severity verifier exits non-zero, or the lint report still shows an error under `react-refresh/only-export-components`, or the resolved editor config no longer reports that rule at error severity, or `allowExportNames` lists any name beyond the four context-module hooks</fails_when>
  </verify>
  <acceptance_criteria>
    - The lint report contains zero errors under `react-refresh/only-export-components`.
    - The resolved editor config still reports that rule at error severity, and its `allowExportNames` list contains exactly the four context-module hook names.
    - `packages/apps/editor/src/components/ContentBoundary/index.tsx` no longer re-exports the wildcard, and `PanelSlot.tsx` imports `NotFoundRecovery` from its real module.
    - `packages/apps/editor/src/components/Table/index.tsx` no longer re-exports `openExternalEditor`.
    - `SelectPointContent.tsx` exports no non-component helper, and `useSelectPointModal.tsx` exports only its hook with the shell living in its own module.
    - `pnpm typecheck` exits 0 and the editor unit suite exits 0.
  </acceptance_criteria>
  <done>Eight sites resolved — four by removing the offending export, four by a name-scoped rule option that leaves the rule's severity untouched — and the choice is documented rather than implied.</done>
</task>

<task type="auto">
  <name>Task 5: Clear the last two errors, absorb any post-01-08 drift, and run the full gate</name>
  <files>packages/apps/editor/src/hooks/useFs.ts, packages/apps/editor/src/runtime/RuntimeProvider.tsx, scripts/verify/lint-severities.js</files>
  <read_first>
    - packages/apps/editor/src/hooks/useFs.ts (the `useCallback` whose first argument is a bound method rather than an inline function, and the overloaded `UpdateFn` type it must satisfy)
    - packages/apps/editor/src/runtime/RuntimeProvider.tsx (the callback that schedules the single automatic retry, and the `start` callback declared later in the same component — read both to see the cycle)
    - .planning/phases/01-baseline-verification-net/01-08-SUMMARY.md (the residual error list this plan has been working through, and whether `AGENTS.md` regeneration was left as a follow-up)
    - .planning/baseline/baseline.json (the editor unit total and the react-monaco-editor flake note)
  </read_first>
  <precondition>Tasks 1 to 4 are complete, so the lint report's only remaining errors are the `use-memo` and `immutability` ones.</precondition>
  <action>
    Two errors are left, and both are in behaviour-sensitive code.

    `packages/apps/editor/src/hooks/useFs.ts`: the rule wants the first argument to the memoization call to be an inline function expression, and the current argument is a bound method. Make the argument inline while producing the same bound behaviour — a small inline wrapper that forwards to the handler's update is equivalent for every call the exported type permits, and the exported type keeps its overloads. If the overloaded update type makes the inline form type-unsafe in a way that changes what callers may pass, switch to the value-memoizing hook form instead with an inline factory, which is the same thing expressed as a value rather than a callback; both forms are behaviour-preserving because the returned function is created once per handler either way, and neither adds or removes a dependency. State in the SUMMARY which form you used.

    `packages/apps/editor/src/runtime/RuntimeProvider.tsx`: the callback that schedules the one automatic retry reads the `start` callback declared beneath it, and the compiler-aware rule reports that forward access. The obvious structural fix — routing the retry through a ref — would change what the retry invokes and when relative to render, in the handshake path the runtime preview tests and the retry-once semantics depend on, which is exactly the kind of change this milestone forbids. Take the narrow disable instead: one line, naming the rule, with a reason that says the retry must invoke the current `start` callback and that breaking the cycle through a ref would change the handshake retry's timing. Do not reorder the declarations, do not change the retry count, and do not touch the timeout.

    Then absorb whatever changed since format-clean was established: run the repo formatter once more so any file created or edited between plans 01-07 and 01-09 (for example a new script) is covered. It is idempotent and formatting-only; confirm `format:check` is green afterwards and that the formatter touched nothing under `.planning/` or `packages/external/`.

    Finally run the complete acceptance set from the root, in one sequence, and record every command's exit code and output figures in the SUMMARY: the root lint, the editor package lint, the format check, the workspace typecheck, the editor unit suite, and the production build with the editor artifact inside the recorded size budget. If the workspace-wide unit fan-out is red only on the known `@motajs/react-monaco-editor` teardown flake with all its tests passing, retry once and say so — do not treat it as a regression and do not fix it here. If the built editor artifact's per-file hashes differ from the baseline manifest, report the size and file count and treat hash drift as expected, since plan 01-08 already documented it.
  </action>
  <verify>
    <automated>pnpm format; pnpm format:check; pnpm lint; pnpm --filter @motajs/editor lint; pnpm typecheck</automated>
    <fails_when>`prettier --write .` or `prettier --check .` exits non-zero, or the root lint exits non-zero, or the editor package lint exits non-zero, or `pnpm typecheck` exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0 and `pnpm --filter @motajs/editor lint` exits 0.
    - `pnpm exec eslint . --format json` reports 0 errors; the warning count is reported in the SUMMARY and is not capped or suppressed.
    - The root `lint` script is still exactly `eslint .` — no `--fix`, no `--max-warnings`.
    - `pnpm format:check` exits 0 and the final formatter pass modified no path under `.planning/` or `packages/external/`.
    - `pnpm typecheck` exits 0, the editor unit suite exits 0 at a total at least equal to the recorded baseline, and `pnpm build` exits 0 with the editor artifact inside the recorded raw budget.
    - On the uncommitted working tree, `git diff -- eslint.config.js packages/apps/editor/eslint.config.js` contains no severity change — the only rule-level change in this plan is the editor's `allowExportNames` option.
    - The SUMMARY records every verification command with its exit code and figures, plus the per-site resolution table for all 42 errors.
  </acceptance_criteria>
  <done>`pnpm lint` is green under both entry points with no rule weakened, formatting is still clean, and typecheck / unit / build all pass — the lint layer of plan 01-07's four-job gate is finally satisfiable.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Requirement |
|---|---|---|
| zero ESLint errors under the root and editor configs | gate state | VERIFY-02 |
| `scripts/verify/lint-severities.js` | repo-owned verifier | VERIFY-02 |
| `allowExportNames` naming the four context-module hooks | narrowed rule option | VERIFY-02 |
| narrow, reason-carrying per-line disables | justified exceptions | VERIFY-02 |
| real fixes at 12 sites (4 mechanical + 2 casts + 4 export removals + 1 inline memo + 1 helper) | source change | VERIFY-02 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lint gate → the repository's merge gate | Once plan 01-07 registers the lint job as a required check, a weakened rule silently un-gates every future change. |
| Editor sources → runtime behaviour | Fixes in `iframeEntry.ts`, `RuntimeProvider.tsx`, `MapPixiRenderer.tsx`, `StatusBarPreviewContent.tsx` and `useImageAssetUrl.ts` sit on live preview and persistence paths. |
| Disable comments → code review | A disable without a reason is indistinguishable from a silenced bug. |
| Test files → module-private state | Two test files reach into private caches to reset or inject state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | making the gate pass by relaxing it | critical | mitigate | `scripts/verify/lint-severities.js` asserts the previously-erroring rules still resolve to their original severity from the parsed config, so a downgrade or disable fails the plan's own check; Task 5 additionally proves `git diff` on both configs contains no severity change and that the `lint` script is still plain `eslint .`. |
| T-09-02 | Tampering | a behaviour change disguised as a lint fix | high | mitigate | `pnpm typecheck`, the editor unit suite and `pnpm build` must stay green at the recorded totals; the behaviour-sensitive React-hooks group defaults to a reason-carrying disable rather than a refactor, and every refactor decision is recorded per site. |
| T-09-03 | Repudiation | reasonless disable comments accumulating | high | mitigate | The verifier walks the tracked tree and fails if any `eslint-disable*` comment lacks non-empty reason text after the separator; the count is printed for the SUMMARY. |
| T-09-04 | Information Disclosure | `--format json` reports written into the tracked tree | low | mitigate | Every JSON report is written to a `node_modules/` path, which is gitignored, so the gate never leaves an untracked artifact behind. |
| T-09-05 | Denial of Service | a stale error list sending the executor to the wrong lines | medium | mitigate | The baseline inventory's coordinates are explicitly declared pre-formatting; every site is relocated by rule id and message from a freshly generated report, and each task's gate is a per-rule assertion on that report rather than a line-number check. |
| T-09-SC | Tampering | package installs (supply chain) | low | accept | This plan adds no dependency and installs nothing; `git diff` on every manifest and on `pnpm-lock.yaml` must be empty for it. |

</threat_model>

<verification>
1. `pnpm lint` exits 0, and `pnpm exec eslint . --format json` reports 0 errors with the warning count printed rather than capped.
2. `pnpm --filter @motajs/editor lint` exits 0.
3. `node scripts/verify/lint-severities.js` exits 0 — named rules unchanged, all disable comments reasoned.
4. `pnpm format:check` exits 0 and no `.planning/**` or `packages/external/**` path is modified.
5. `pnpm typecheck` exits 0, the editor unit suite exits 0 at or above the recorded total, and `pnpm build` exits 0 within the recorded size budget.
6. On the uncommitted working tree, `git diff -- eslint.config.js packages/apps/editor/eslint.config.js` shows no severity change and no new ignore path — the only rule-level change is the editor's `allowExportNames` option.
7. On the uncommitted working tree, `git diff --name-only -- pnpm-lock.yaml` and the same for every `package.json` produce no output, proving no dependency was added.
8. The SUMMARY carries a per-site table for all 42 errors: site, rule, resolution (real fix or reason-carrying disable), and the verification command that proved it.
</verification>

<success_criteria>
- `pnpm lint` and `pnpm --filter @motajs/editor lint` both exit 0 with warnings permitted and errors at zero.
- Every one of the 42 non-formatting baseline errors is resolved, with the resolution recorded per site.
- No rule was weakened anywhere: severities unchanged, no new ignores, no warning cap, and every exception is a single-line reasoned disable.
- No behaviour changed: typecheck, unit suite and build all stay green at the recorded figures.
- The lint layer of plan 01-07's four-job PR gate is satisfiable, which is what finally unblocks publishing it.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-verification-net/01-09-SUMMARY.md` when done. It MUST record: the per-site resolution table for all 42 errors (site, rule, real fix vs reason-carrying disable, reason text where used); the total disable-comment count printed by the verifier; the per-site justification for the seven `set-state-in-effect` decisions and the `immutability` disable; which form was used for the `useFs.ts` memoization fix; the `allowExportNames` names and the reason the four context-module hooks were resolved that way; the final `pnpm lint` warning count; and every verification command with its exit code, including the editor artifact size beside the baseline figure.
</output>
